# BMB 当前状态

> 每次开工先读这份（CLAUDE.md 指定入口）。历史流水/早期调试记录归档在 `.planning/mcgm-main/`（status_summary、findings、progress），只读参考，不再更新。

## 当前进度（2026-06-16）

- `bmb_sheep` 切片（mock 世界）：生成 ✅、Flee ✅（**第九轮 MC PanicGoal 式重写，用户已实测通过**：随机近点 dash、没路放弃、平地不跑远）、prop 物理伤害 ✅、速度锯齿 ✅、倒退 ✅、吃草频率 ✅、MC 游荡节奏 ✅、转圈 ✅、跳崖 ✅、贴 prop 冻住 ✅、障碍犹豫掉速 ✅。
- **第十轮（待复测）：RealBlockWorld 接通 MCSWEP，mock 首次切真环境**。朋友的 `MC.SV.SetBlock` 已就位（addon 在 `D:\...\addons\mcswep-main`，接口文档在其 `docs/interface-usage.md`，签名已对照源码 `mc/sv_world.lua:392` 验证）。目标：羊在真方块世界跑通"游荡 → 受击逃 → 吃草 grass_block→dirt"全链路。
- **第十一轮（用户已验证）：修"一格宽走廊出得来、进不去"**。根因在路径跟随层，不是 A*：`MoveAlongPath` 对 A* 路径每 tick 又跑 `IsMovementTargetSafe`，36u 走廊入口会被 `WallStopDistance=20` 的 Source hull 探测误判成墙；carrot 也可能从当前位置直线瞄向前方节点/终点外投，直角入口切角撞墙。修法：A* 路径跟随不再用 Source 安全探测二次否决；`GetPathCarrot` 改成 pure pursuit（先投影到当前路径折线，再沿折线前推）；再用 `IBlockWorld.IsSolid` 做网格视线检查，直线到 carrot 被方块挡住时二分缩短到最后可见的折线点。裸方向移动/legacy/debug direct 仍保留 `IsMovementTargetSafe`。
- **第十二轮（用户已验证）：修 debug 目标秒退 + 羊从一格高洞/方块角穿过**。上一轮仍按"中心点所在格"判断通行，没按成年羊宽高占格，所以会从 1 格高天花板下钻、从方块角擦过去；Tool Gun 右键也仍走 direct debug move，不是 A*。修法：`bmb_base_mob` 新增 `IsBMBHullClearAtPosition`，按实体水平半径和身高检查周围实心格；A*、随机候选、carrot 网格视线统一用 mob hull 通行；`bmb_sheep` hull 调到 32u 宽（MC 成年羊 0.9 格约 32.4u）；Tool Gun 右键目标改走 `MoveToWorldPosition` A*，点击面会略抬/推出到可站立空气格。
- **第十三轮（用户已验证）：修拐弯漂移/打滑**。用户确认第十二轮三项通过后，反馈方块走廊拐弯有惯性滑出去再反复矫正。修法：`MoveAlongPath` 新增 `path_corner` 过弯控制，提前约 2 格检测折线急转，靠近转角时动态降低目标速度、缩短 carrot 到约一格内，并临时提高 `loco` deceleration；直线段恢复 `path_carrot`。
- **第十四轮（用户已验证）：修 Source 地图墙/跳崖回归**。退役 path 里的 Source 安全复查后，A* 只知道 MC 方块，不知道 gm_flatgrass 地图墙和平台边缘，导致羊会沿 `path_carrot` 往地图墙/悬崖走。修法：`MoveAlongPath` 加回 path 专用 Source safety：前向 hull 命中若对应 `IBlockWorld` solid 方块则忽略（不误伤一格方块走廊），若不是方块 solid（地图墙/prop）则 `path_wall`；前向地面探测没地/坡太陡/落差 > `MaxStepDown` 则 `path_cliff` 并急刹。
- **第十五轮（用户已实测，发现三个 bug → 第十六轮）：A* 3D 邻接 + BlockHop / drop 边**。`sv_pathfinder` 在 real 方块世界生成 `walk` / `hop` / `drop` waypoint：+1 格高差标 `hop`，向下 ≤3 格有支撑落点标 `drop`；mock 仍固定平面。`MoveAlongPath` 新增 `path_hop` / `path_drop`，hop 用 45u 顶点的 `loco:SetVelocity` 弹道，空中弱控水平速度；drop 不跳，故意走出边缘让重力落下。hop/drop 消费期间豁免 `path_wall` / `path_cliff`，避免台阶被当墙、下落边被当跳崖；终点判定补上"垂直边必须落地且脚部 cell 到目标层"。real `GetRandomWalkablePoint` 开始抽当前层附近的可支撑候选，让 Wander 有机会自然跨上/跨下真实起伏地形。
- **第十六轮（用户已实测：不卡了、不跳楼了、下落正常 ✅；遗留三个新问题 → 第十七轮）：修 hop 贴墙不跳、右键高处/非 MC 地面整帧卡顿且不动**。
  - **hop 贴墙卡死**：根因是 `MoveAlongPath` 每个 hop 节点只跳一次（`hopStartedAt` 永不复位），第一跳撞方块面落回后永远走空中弱转向把羊往方块上蹭，且该分支每 tick 续 no-progress watchdog，连兜底失败都进不去；加上起跳"只保留水平速度"，贴墙时水平≈0，跳起来直上直下必然失败。修法：落地未推进节点超过 `BlockHopRetryDelay`(0.25s，必须 < watchdog grace 0.35) 复位重跳，连续 `BlockHopMaxAttempts`(3) 次失败按 `path_hop_fail` 交还行为层；`StartBMBBlockHop` 朝目标方向的水平分量不足行走速度时补足（已有横向速度照旧保留）；空中转向只在真离地时接管。
  - **A* 不可达泛洪 = 卡顿根因**：同层 walk 边过去只要求"可通行"不要求脚下有支撑，目标格悬空（点高处墙面被推出的空中格）或落在非 MC 地面（drop 边过去要求落点下方必须是 MC 实心，flatgrass 地皮不算）时目标永远不可达，搜索顺着空中格淹整片空域扫满 900 次迭代，每格又是十几次 `MC.GetBlock` 的 hull 扫描且无缓存——全在一帧 = 卡一下，然后返回 nil = 不动。修法五件套：① `IBlockWorld.HasSupport`（real：先查 MC 实心纯表查询，无实心才兜底一次 `MASK_SOLID_BRUSHONLY` TraceLine 认 Source 刷子地面，StartSolid=整格埋地不算；prop 不算支撑）；② 同层 walk 边要求 `isStandable`（passable+support），搜索空间锁死在真实可走表面，路径也不再可能悬空；③ 目标格悬空向下吸附 ≤12 格到第一个可站立格（点高墙面 = 走到墙根），吸不到快速失败；④ passable/support 进 per-FindPath 缓存（同格同次搜索只算一次）；⑤ 搜索预算 `f = g+h ≤ hStart*2+24`（椭圆界，"无路"结论不再扫满上限）+ 每 64 次迭代 `coroutine.yield()` 时间切片（大搜索摊到多 tick，单帧不卡）。预算和 yield 互补：预算管总开销，yield 管单帧。
  - **部分路径**：搜索中止时返回离目标最近已展开节点的路径（标 `waypoints.partial = true`，至少要比起点更接近目标才返回）。行为观感从"卡顿后拒动"变成"走到崖边/尽头停住"（原版手感）；高于 3 格的垂直落差仍按 MC 规则拒跳（要改上限调 `MaxPathDropCells`）。**Flee 显式 `allowPartial=false`**：partial 会把"撞墙"洗成"成功冲刺"，失败计数永远清零，破坏第九轮已验证的"被围住冲几下放弃"。
  - real `GetRandomWalkablePoint` 候选支撑判断统一走 `HasSupport`：站在高台上抽到平台外的同层悬空格在源头就被拒，不再依赖 A* 失败兜底。
- 另：用户已确认旧清单三项通过 ✅：枪击受伤 Flee 稳定触发；高速 prop 砸中/砸死沿原方向衰减不反弹；真方块世界全链路（游荡含走下一格地板 → 受击逃 → 吃草 grass_block→dirt 同步/音效/存档；`bmb_world mock` 回退正常）。
- **第十七轮（用户已实测：绕路不再误杀 ✅、窄沿恢复可走 ✅；hop 仍不起跳 → 第十八轮）：修第十六轮实测的三个新问题**。
  - **`path_no_goal_progress` 误杀绕路**（迷宫里走到一半、前面明明有路却放弃）：该 watchdog 要求每 0.9s 离终点直线距离至少近 10u，但绕墙的合法路径必然有"越走离终点越远"的段。修法：沿路径推进节点视为实打实进展，节点推进时刷新 watchdog（baseline 距离 + deadline）；watchdog 保留原本"防原地绕圈"用途（绕圈不会持续推进节点）。`PathGoalProgressTimeout` 0.9→1.2 给过弯减速段留余量。
  - **窄 Source 路完全走不了**（flatgrass 围墙窗台，上一轮还能走）：第十六轮回归——walk 边要求支撑后，`HasSupport` 的 Source 兜底只在格子中心打一条线，窄沿比 36u 格子窄、MC 网格中心可能正好悬在沿外侧 → 整条沿被判无支撑。修法：中心 StartSolid 仍判埋地不可站；中心悬空没命中时补 4 个轴向 ±12u 偏移采样，任一命中（非 StartSolid）算支撑。
  - **`path_hop` 状态有了但人不起跳，试几次放弃**：第十六轮回归——起跳那个 tick 还在地面，改成走 `SteerTowards`→`loco:Approach` 后，把 `SetVelocity` 直写的竖直速度在物理生效前冲掉了（第十五轮起跳 tick 走的是空中分支所以至少跳得起来）。修法：新增 `BlockHopLaunchWindow`(0.15s，必须 < RetryDelay 0.25)，起跳保护窗内强制走空中转向不许 Approach；窗口过后落地贴墙仍交回 SteerTowards + watchdog（保留第十六轮的防卡死语义）。
- **第十八轮（用户已实测：hop 有抬脚/离地但一陷一陷仍上不去；物理枪好很多但仍轻微弹簧抽动 → 第十九轮）：hop 真根因第一版——落地态 `loco:SetVelocity` 写竖直速度无效**。用户四张截图证实：整块方块的 hop 从未真正离地（hop2/3 有状态无动作）；半砖两次"跳跃成功"其实是 `StepHeight=28 > 半砖18` **走**上去的（hop4：明明能走上去却显示跳跃失败、人却已在上面）。NextBot 落地态的地面解算会把直写的竖直速度当帧压回地面，保护窗（第十七轮）治标没治到本。修法：`StartBMBBlockHop` 先 `loco:SetJumpHeight(apex)` + `loco:Jump()` 把 locomotion 切进跳跃态，再 `SetVelocity` 覆盖成固定弹道（45u 顶点 + 朝目标补足的水平分量）。CLAUDE.md 的 BlockHop 约定同步改。另查证 MCSWEP 源码：`BlockIsFullCube` 对半砖返回 false → BMB 把半砖当空气，A* 看不见半砖，混半砖地形会选择跳整格而不是走半砖台阶——观感问题，遇到再用 `MC.BlockBoxes` 细化。
  - 顺手拔掉两根刺（另一 Fable 建议）：① 0.15s 起跳保护窗删除——`loco:Jump()` 同步置跳跃态，空中分支直接查 `loco:IsClimbingOrJumping()`（起跳当帧强制视为真，防引擎标志晚一帧），不再留"窗口与真实物理不一致谁说了算"的隐患；② 重跳延时改以 `OnLandOnGround` 回调时刻为基准（`BMBLastLandTime`），不靠 `IsOnGround` 轮询猜时序。
  - **新增物理枪持握一等状态 `BMBHeld`**（用户反馈：抓起来的羊有的上下抽搐/陷地、有的安静悬挂——取决于被抓瞬间 loco 醒/睡，醒的会和物理枪持握点拉扯）。`PhysgunPickup`/`PhysgunDrop` 钩子 → `OnBMBPhysgunPickup/Drop`：持握期间 base Think 每 tick `loco:SetVelocity(vector_origin)` 缴械、羊行为循环挂起（state=held）、`MoveToWorldPosition`/`MoveAlongDirection` 拒新请求；拾起时 `InterruptBMBMovement` 掐掉当前 move（hop 重跳计数等局部状态随协程销毁，不会后台数失败）；松手 `SetVelocity(0,0,-10)` 踹醒睡眠 loco，挂半空的正常下落。
- **第十九轮（用户已实测：物理枪上下抽完全修好 ✅；hop 仍低弧/概率上台/擦模 → 第二十轮）：BlockHop 改用 `loco:JumpAcrossGap` + 物理枪 held gravity/desired speed 归零**。第十八轮 `Jump()+SetVelocity` 让脚离地但表现为节律性小陷跳，符合"竖直速度起效一两 tick 又被 locomotion/地面解算压回"的签名。修法：`StartBMBBlockHop` 优先调用 NextBot 原生 `loco:JumpAcrossGap(landingGoal, landingForward)`，落点给上层脚部格的地表点（foot cell center z - 半格），`SetJumpHeight` 抬到至少 58u；原生 hop 期间只转向/刷新 watchdog，不再 `SetVelocity` 弱控水平，避免和原生跳跃解算抢方向盘。没有 `JumpAcrossGap` 的旧引擎才 fallback 到 `Jump()+SetVelocity`。物理枪 held 进一步每 tick `SetGravity(0)` + `SetDesiredSpeed(0)`，松手恢复原 gravity，压掉残留弹簧感。
- **第二十轮（用户已实测：debug 有助跑能上；wander 靠近常上不去；native 近距离多次 `apex=0`，偶发成功 `apex=36` → 第二十一轮）：BlockHop 起跳准入 + 落点/高度余量 + 分类 HUD**。用户截图显示失败处已进入 `path_hop`，且成功需要反复 debug 点击、弧线很低并会擦进模型，说明主要问题在跟随层触发点/速度/落点，不是 A* 完全没标 hop。修法：① `JumpHeight` 写成 `1.6 * BlockSize`（约 58u）；② `JumpAcrossGap` 落点改为上层格中心、z = 台面 +2u；③ 起跳准入窗口：2D 距离约 `0.85~1.4` 格，朝目标速度 ≥ `0.6 * pathSpeed`，太近/太慢先退到约 `1.15` 格的 backoff/助跑点再跳；④ Debug HUD 增加 `hop# native/manual d face v apex result`，并提供 `bmb_debug_hop_log 1` 控制台日志，把"概率"拆成起跳距离、速度、实际顶点和成败。
- **第二十一轮（用户已实测：drop 主动下 3 格内 ✅；hop 仍全部失败 → 第二十二轮）：BlockHop 改错帧手写弹道 + Wander 主动下层采样**。第二十轮 HUD/日志结论：`JumpAcrossGap` 在 `dist≈36/face≈18/speed≈50` 的近距离爬台多次 `apex=0`，只有 `dist≈47/face≈29/speed≈73` 的助跑样本成功且 `apex≈36`，所以 native jump 对一格爬升不可靠。修法：默认不再调用 `JumpAcrossGap`；先 `loco:Jump()` 打开跳跃态，下一 tick 按弹道公式 `SetVelocity`（竖直顶点 `1.6*BlockSize`，水平速度 = 距离/飞行时间并 clamp 到 `32..1.1*pathSpeed`），速度写入后给一个极短空中控制窗口，避免同一轮 path loop 又把它交回 `Approach` 压掉上抛；同时取消“必须已有 0.6×pathSpeed”的起跳硬门槛，wander 慢速靠近也能跳。实测日志显示 `hop velocity ... vz≈339` 已写入，但 `apex=0~12`，说明问题不是速度数值，而是水平过早顶住方块侧面/地面解算把上抛磨掉。另查 MC 源码：`Entity#getMaxFallDistance()` 默认 3，`LivingEntity#getComfortableFallDistance(0)=3`，`Mob` 无目标时用 3，羊未覆写；BMB 的 `MaxPathDropCells=3` 保持不变，real `GetRandomWalkablePoint` 下层偏置已让 Wander 主动下落生效。
- **第二十二轮（用户已实测：一格高 BlockHop 成功 ✅）：BlockHop 两段式 manual hop**。`ApplyBMBPendingBlockHop` 不再立刻给水平速度，而是建立 `BMBActiveBlockHop`：第一段 lift 只给竖直速度，短窗口内如果仍在地面就重复 `loco:Jump()`；达到约 `0.8*BlockSize` 抬升或超过 lift 时间后，第二段再加水平速度/弱转向落到上层格。实测日志 `apex≈54~65`，NPC 已能跳上一格台阶。保留问题：弧线偏高，偶发能误上两格（A* 不会主动规划两格 hop）；跳完动作会多保持一段时间，套皮后要复查观感。
- **第二十三轮（用户已实测：当前未发现 bug ✅）：StepHeight / timeout / activity 收口**。按 Fable 诊断，误上两格是 apex≈55 与空中 `StepHeight=28` 叠加：脚部高度超过 `72-28=44` 后落地解算会 step 上两格台沿。因此不削掉一格 hop 需要的 apex，而是在 hop 状态临时把 `loco:SetStepHeight(18)`，成功/失败/中断/路径结束恢复默认 28。debug 右键长路径不再把面板 duration 当硬截断：路径 timeout 改为路径长度 / 速度 × 系数 + base，仍由 no-progress watchdog 判断真卡住。跳后动作残留改为 Think/落地的状态驱动 activity 选择，落地后按速度回 idle/walk/run。用户复测：一格 hop、误上两格、debug 长路径、跳后动作均未再暴露问题。
- **第二十四轮（代码已切：36.5 / BS 参数化，待用户游戏回归）：MCSWEP 已把 `MC.Config.BlockSize` 切到 36.5，BMB 同步收口到单一尺寸入口**。`BMB.GetBlockSize()` 每次优先读 `(MC and MC.BS)`，mock fallback 改 36.5，并同步 `BMB.BS` / 兼容字段 `BMB.Config.BlockSize`；业务代码不再直接读 `BMB.Config.BlockSize`。BaseMob 的尺寸派生默认改为 scale/cell：goal/node tolerance = `0.5*BS`，carrot min/max = `2*BS` / `25/6*BS`，corner slow = `2*BS`，hop apex/jumpheight = `1.5*BS`，manual lift = `0.8*BS`，hop 临时 StepHeight = `0.49*BS`（36.5 时约 17.9 < 半砖 18.25），`MaxStepDown = 1.1*BS`；默认 StepHeight=28 保留为 Source locomotion 绝对值（仍 > 半砖、< 整格）。Sheep 的 wander/flee 半径改用 cell 数（3~8 格、panic 5 格、min 1 格）。Real/Mock block world、debug HUD/tool、HasSupport 轴向偏移也改为跟随 BS。新增 `scripts/check_block_size_parameterization.ps1` 防止旧尺寸 fallback 回归。已从朋友源码确认 `D:\...\mcswep-main\lua\mc\sh_config.lua` 中 `MC.Config.BlockSize = 36.5`。
- **第二十五轮（代码已切：StrandedRecovery，待用户游戏回归）：半砖/MC 台阶走不上是表面高度接口缺失，不在本轮硬修；先解决玻璃板/脚下支撑失效导致的非法起点冻结**。新增 `BMB.Pathfinder.IsStandablePosition` / `FindNearestStandable`，BaseMob 在 `IsOnGround` 且当前脚部 cell 非 standable 时进入 `stranded` 状态。复测发现两版问题：① 只检测不够会卡在 `stranded_no_target`；② 大半径搜索 + 远点 direct 会卡顿，并让实体沿玻璃板网络走下去。现改为本地 bail-out：采样周围 8 个短距离点，优先走到邻近合法 standable 点；否则选择没有物理支撑的一侧轻推下去，进入 `stranded_fall` 后靠重力落回合法地面。普通 Wander/Flee A* 仍不会主动把玻璃板顶、栅栏顶等窄碰撞规划为合法节点。新增 `scripts/check_stranded_recovery.ps1` 防止调度/bail-out 语义回退。
- **第二十五轮补丁（代码已切，待用户游戏回归）：站在 GMod prop 上不再误触发 StrandedRecovery**。`HasSupport` 仍然不把 prop 当 A* 地形；BaseMob 只在当前位置用 `IsBMBOnPropSupport()` 识别 `prop_` / `func_physbox` / VPhysics 实体支撑，并在 `ShouldRunBMBStrandedRecovery` 前直接放行，避免 HUD 卡在 `stranded_no_escape`。如果从 prop 起点 A* 找不到合法路径，`MoveToWorldPosition` 只启用短 `prop_direct` 兜底，仍由 `IsMovementTargetSafe` 的 Source hull/ground trace 处理 prop 边缘（`cliff`）和墙，不把 prop 顶面写进寻路语义。
- **第二十六轮（用户已实测核心移动通过）：StrandedRecovery 卡障碍、drop 空中回头、hop 起跳对齐、多 mob 性能首轮优化**。玻璃板上撞障碍后不再永久 `stranded_bail_blocked`：记录失败方向短冷却，HUD 进入 `stranded_bail_retry`，下一轮换方向。`path_drop` 离边后改用 `MaintainBMBDropAir`，不再 FaceTowards/air-steer 追 carrot，避免高处下来空中回头转一圈。BlockHop 起跳新增横向对齐窗口，横向偏离或距离过远时先走 backoff/launch 点，不直接冲方块面“试一下”。用户确认玻璃板撞障碍、高处 drop 回头、复杂台阶 hop 对齐均通过；遗留 drop 水平惯性和性能。
- **第二十七轮（用户已实测功能通过，发现移动一卡一卡 → 第二十八轮）：drop 水平惯性、debug partial/hop 早停、新出生 idle、多 mob 峰值优化**。`MaintainBMBDropAir` 保持不转身，但把空中水平速度钳到目标速度的一小段，避免从高处掉下时被完整行走惯性带太远。debug 右键 path 不再一次 `MoveToWorldPosition` 后无论成败都清空命令：到达目标或过期前持续 replan，partial/dead-end/hop 失败只进入 `debug_repath` 短暂停顿。新生成 sheep 默认 `idle` 4~9s，debug/stranded/flee 仍优先。性能分摊保留：physics impact 周期查找 `0.3s` 且生成时错峰，A* `PathfinderYieldEvery=24`，Wander 单轮最多 2 次完整路径尝试并对失败随机退避。
- **第二十八轮（代码已切，待用户游戏回归）：恢复 NextBot entity Think 每 tick，修第二十七轮一卡一卡回归**。根因：把整个 `Think()` 调度改成 `NextThink(CurTime()+0.2)` 等于把 NextBot 实体更新降到 5Hz，loco/身体插值表现为走路一顿一顿。修法：`Think` 末尾恢复 `NextThink(CurTime())`；性能优化不再节流实体 Think，只保留内部贵操作的节流/错峰（physics impact、A* yield、Wander attempts/failure pause、spawn idle）。`scripts/check_drop_debug_spawn_perf.ps1` 和 `scripts/check_movement_recovery_and_scaling.ps1` 已改为禁止 `NextThink(CurTime()+self.ThinkInterval)` 回归。
- **第二十九轮（代码已切，待用户游戏回归）：hop 贴脸反复失败、debug 半路过期、carrot 跨一格空**。用户日志显示失败 hop 多为 `face≈16~20/speed≈0/apex=0`，成功样本为 `face≈31/speed≈111/apex≈50`，根因是起跳准入只看中心距，允许 hull 已贴近方块面时硬跳；现新增 `BlockHopLaunchMinFaceDistanceScale=0.75` / `IdealFaceDistanceScale=0.85`，`face_close` 时先退到更远 launch 点再跳。debug 右键目标默认命令寿命改为约 120s，并在推进节点/靠近目标时用 `DebugPathProgressGrace` 续命，避免复杂路径跑一半过期回 wander。carrot 直线可见从“没有 solid 挡住”升级为“沿线 hull clear 且每个采样点 standable”，防止路径跟随为抄近路跨一格空洞；新增 `scripts/check_hop_debug_gap_regressions.ps1`。
- **第三十轮（debug 已验证通过；碰撞计划后续撤销）：debug gap 假死 + MC 式碰撞试验第一版**。用户确认普通 path 已不会跨一格空，但 debug 期间走到空洞/死路前会卡在 `debug_repath`。根因是第二十九轮把 debug 命令寿命放长后，缺少命令级“无进展”出口：一段段失败会持续到 120s。现保留 `DebugPathNoProgressTimeout=3.5`，debug path 只把“推进节点/明显靠近目标”算进展并续命；连续无进展则 `debug_no_progress` 急停并清掉命令，回到普通行为。碰撞方面曾尝试 `COLLISION_GROUP_PLAYER` + 软分离，但后续实测会继续引出硬支撑/物理交互问题，已撤销。
- **第三十一轮（已撤销）：禁用 player/BMB mob 硬碰撞试验失败**。用户复测确认第三十轮 debug gap 修好 ✅，但碰撞试验导致物理枪抓不起、子弹不掉血，并和 prop 物理伤害链路冲突。结论：不继续做 MC 式实体穿插，保留 GMod/NextBot 默认手感。代码恢复 `SetCollisionGroup(COLLISION_GROUP_NPC)`，删除 player-like collision group、`SetCustomCollisionCheck`/`ShouldCollide` hook、软分离函数与 Think 调用；`scripts/check_debug_gap_and_collision.ps1` 改为防止碰撞试验代码回归，同时继续保护 debug gap 修复。
- **第三十二轮（代码已切，待用户游戏回归）：Flee 速度/动作意图稳定化**。用户反馈 flee 状态速度不稳定，后续套皮会导致羊动作异常，也会污染 Base。根因是 Flee 虽然以 `RunSpeed` 调 `MoveToWorldPosition`，但通用 `path_corner` 会把瞬时 path speed 降到 `RunSpeed*0.55`（sheep 约 49.5），低于 walk/run 阈值（约 80），导致 HUD 目标速度和 activity 在跑/走之间波动。现 Base 新增 `BMBActivitySpeed`：`BMBDesiredSpeed` 继续表示 loco 当前命令速度，`BMBActivitySpeed` 表示行为/动画意图速度；activity 选择改用意图速度。Flee 传 `moveIntentSpeed=RunSpeed`，并用 `minPathSpeed` 把过弯临时降速夹到 run 阈值以上，既保留 corner 控制，又不让 panic/run 动画抖。新增 `scripts/check_flee_speed_stability.ps1`。
- **第三十三轮（代码已切，待用户游戏回归）：受击红闪 / 伤害冷却 / 水平击退 + Flee 连击不重选方向**。MC 源码确认 `hurtTime = 10 ticks`；`invulnerableTime` 字段会设为 20，但 `LivingEntity` 只有 `invulnerableTime > 10` 时挡同等/更低伤害，所以 BMB 对应红闪 0.5s、有效伤害冷却 0.5s。`OnTakeDamage` 只有在非冷却窗口内才扣血、开启客户端红色调制、触发 flee 和击退；冷却内重复命中 `return 0`，不刷新 flee。击退是一等状态：sheep 调度顺序为 held → knockback → debug/stranded/flee，击退窗口内普通 path/steering 拒绝新移动，水平速度按来源方向重置并 capped，不和旧速度累加；`DMG_CRUSH` 物理砸击保留原 GMod/prop 伤害链路，不叠 BMB 击退。Flee 中受击会刷新恐慌窗口/威胁来源，但不再额外 `InterruptBMBMovement` 当前 flee 段，避免连续受击时反复重选目标方向导致原地犹豫。新增 `scripts/check_damage_iframes_knockback.ps1`。摔伤仍是待办，本轮不实现。
- **第三十四轮（代码已切，待用户游戏回归）：修受击后 `vel:70/0` 和击退不生效**。用户实测红闪/无敌帧正常，但每次闪红都会让羊静止，HUD 显示当前速度/目标速度如 `70/0`，且看不到击退。根因不是红闪渲染越界：`StartBMBHurtFlash()` 只写红闪 NWFloat；真正把第二个数写 0 的是 `RunBMBKnockback()` 中的 `MaintainBMBMoveSpeed(0)`。这既把 HUD/行为意图速度变成 0，也可能让 `loco:SetVelocity` 的水平击退被 desired speed 0 吞掉。修法：击退状态不再发布 `BMBDesiredSpeed=0`，而是保存受击前的 `BMBKnockbackDesiredSpeed` / `BMBKnockbackActivitySpeed` 供 HUD/动画继续显示原意图；内部单独给 loco 一个 `BMBKnockbackLocoSpeed` 预算以允许水平 `SetVelocity` 生效，并在击退启动当帧立即写一次水平速度。新增检查确保红闪函数不碰 movement/loco，且 knockback runner 不再写 `MaintainBMBMoveSpeed(0)` / `SetDesiredSpeed(0)`。
- **第三十五轮（用户已验证）：击退手感对齐 MC：0.5s 冷却、短窗口、竖直上抬、空中继续 flee**。用户复测确认 `vel` 不再变 0，但受击后仍有停顿；MC 26.1.2 实测/源码显示有效无敌约 0.5s，受击会有上抬离地，且空中仍尝试逃跑。修法：`DamageInvulnerabilityTime` 改 0.5；`KnockbackDuration` 从 0.35 缩到 0.12，只保留防 steering 吞冲量的短仲裁窗口；新增 `GetBMBKnockbackVerticalVelocity()`，地面受击用 `loco:Jump()` 开跳跃态后给约 `6*BS`、clamp 到 170~240u/s 的竖直速度，空中受击保留当前 z；Flee 在 knockback 结束后若仍在空中，传 `allowStrandedStart = airborneStart`，让 A* 从 unsupported/airborne 起点继续尝试逃跑，而不是原地等落地。
- **第三十五轮后 hotfix（代码已切，待用户游戏回归）：Sheep Flee 目标速度固定满速并延长恐慌窗口**。用户反馈 sheep Flee HUD 目标速度有时是 81、有时是 90，且 flee 时间偏短。诊断：`81` 来自旧的 path corner 下限公式 `(WalkSpeed+RunSpeed)/2 + 1`，sheep 原来 70/90 时阈值正好约 81；直线段显示 90，拐弯/局部安全控制时显示 81。现共享 Flee 支持 `FleeKeepFullSpeed=true`，sheep 开启后 `minPathSpeed=RunSpeed`，并把 `RunSpeed` 调到 100、`FleeDurationMin/Max` 调到 3.5/5.0s；其他 mob 未设置该开关时仍保留原来的跑步阈值下限语义。
- **模型动画管线补丁（代码已切，待首个模型回归）：BMB 支持 per-mob sequence 映射**。正式 MC 模型不再只能靠 `ACT_*` 或 sheep 临时骨骼摆腿。BaseMob 新增 opt-in `AnimationSequences`：每只怪把逻辑动作映射到转换器导出的 sequence 别名，例如 `{ idle="idle", walk="walk", run="walk", attack="attack", hurt="hurt", death="death" }`；BMB 用 `LookupSequence` 缓存查找、`ResetSequence` 切换、`SetPlaybackRate` 按当前水平速度缩放 walk/run。目标序列不存在时回退 idle，idle 也不存在才回到旧 Activity 层；未设置 `AnimationSequences` 的旧实体行为不变。新增 `scripts/check_sequence_animation_adapter.ps1`。
- **Sheep 模型动画接线临时撤回（代码已切，待游戏回归）**。BaseMob 的 per-mob `AnimationSequences` 适配器保留，但 `bmb_sheep` 的 `AnimationSequences` / `AnimationReferenceSpeeds` 先注释掉，等转换器 pivot 修好、低速 playback/cycle 问题解决后再打开。客户端 `UpdateBMBVisualBones` 保留 Base 通用 `UpdateBMBLimbSwing` 的连续速度缩放，并把腿部程序化摆动接回；当前 `legSwingMax=25.0`、`LimbSwingPhaseScale=0.13`，走/跑频率都比默认 0.18 更低。不恢复旧 `speed>8 then` 硬分支或 `rate` 计时摆腿。头部 locomotion swing 已关闭，普通移动只在退出吃草/preview 后清一次旧 head pose，不每帧锁死 head 骨；吃草/后续看向系统仍可接管头部。
- **第三十六轮（代码已切，待用户游戏回归）：Zombie Phase 1 迁移到 BMB Base**。新增 `bmb_zombie.lua`，继承 `bmb_base_mob`；旧 `mcgm_zombie.lua` legacy 样机已在 v1 发布前删除，不再进菜单。`sv_behaviors.lua` 新增可复用 `SeekTarget` / `Chase` / `MeleeAttack`，Zombie 状态机只负责参数、声音、目标状态和调度优先级。调度顺序与 Sheep 一致：held → knockback → debug → stranded → hostile AI；追击每约 0.35s 用 BMB 方块 A* 追玩家当前位置（`skipSourcePath=true`），近战用 `DamageInfo` + windup/cooldown。
- **第三十七轮（代码已切，待用户游戏回归）：Zombie 首轮实测修正**。用户发现：远处能进 `chase` 但不明显追击；追击时 Zombie 脚不动；高低落差时卡在 `attack_ready` 往上看；攻击距离太贴脸；红闪有渐变而 MC 是命中后直接红。修法：`ChaseSegmentTimeout=1.0`，避免 0.35s 段太短还没推进就重置；`MeleeAttack.IsInRange` 增加独立 `AttackVerticalRange`，Zombie 攻击距离改 52u、垂直攻击范围 28u，高一整格时继续寻路而不是 `attack_ready`；Classic zombie `RunActivity=ACT_WALK`，追击用模型可靠走路序列；Base 红闪改为整个 `HurtFlashTime=0.5s` 内固定红，不再按剩余时间渐变。
- **第三十八轮（代码已切，待用户游戏回归）：Zombie 攻击/追击连续性 + final hop 误完成**。用户继续实测：站高两格时 Zombie 会停；远处追击会走几步停一下；攻击速度偏慢且挥击时 HUD 速度变 0；复杂台阶显示 `path_hop` 但没触发跳跃，随后 `idle dist:0`。修法：近战不再设置 `BMBMeleeLockUntil`，也不再 `MaintainBMBMoveSpeed(0)`；Zombie `AttackCooldown=0.8`、`AttackHitDelay=0.28`、`AttackMoveSpeed=92`，攻击/attack_ready 期间继续朝目标前压；追击失败但目标仍有效时保持 target，进入 `chase_repath` 短暂停顿重算，不清空目标回 wander；Base 新增 `IsBMBVerticalPathNodeReached`，hop/drop final 和节点推进必须验证实际脚底高度，避免 2D 贴近但没跳上去就把 final hop 当成功。
- **第三十九轮（代码已切，待用户游戏回归）：Zombie chase 停顿 + 贴脸 hop 兜底**。用户复测确认攻击速度/攻击时 HUD 速度已正常，但远处仍会走一会停一下；台阶前会在 `path_hop` / `chase_repath` 间切换，贴近一格台阶不跳；高两格贴脸仍不移动。修法：Zombie `ChaseSegmentTimeout` 1.0→2.0，`ChaseFailureRepathDelay=0.05`，`TurnInPlaceAngle=170`，减少重规划/转身造成的肉眼停顿；`chase_repath` 等待期间继续 `SteerTowards` 目标并 `BodyMoveXY`，不纯站住；Base hop launch 增加可选 `BlockHopAllowCloseLaunch`，Zombie 开启后在狭槽/贴脸无 backoff 空间时允许 `close_lift` 起跳，仍走两段式“先竖直抬升、再水平前压”的弹道，不回到旧贴脸硬冲。
- **第四十轮（代码已切，待用户游戏回归）：Zombie MC-style 视线直追 + 高处不可达贴底等待**。用户继续反馈：远距离仍会走走停停，且平地追击不像原版 MC 那样盯着玩家直压；玩家站高处且没路时，Zombie 应在下面贴着/徘徊等待，而不是假装 `chase_repath` 往零向量移动。修法：共享 `Chase` 增加三层策略：① `chase_direct`，目标可见且前方短探测安全时，每 tick 直接 `FaceTarget + SteerTowards(player)`，开阔地不走 A* carrot；② 视线断开、前方墙/悬崖、迷宫/绕路时回落到原 BMB 方块 A*，保留 hop/drop/partial path 能力；③ A* 失败且目标在近处高处时进入 `chase_stalk`，保持 target、面向玩家、短周期重查，不再清目标 idle，也不再对零水平向量 `SteerTowards`。Zombie 开启 `ChasePreferDirect`，并用 `ChaseDirectProbeCells=4` 保留前方墙/悬崖安全检查。
- **Zombie Phase 2（代码已切，待用户游戏回归）：即时近战 + MC ambient 叫声**。用户要求 Zombie 进入攻击范围立即出手，只保留约 0.75s 攻击间隔；命中玩家要有击退、少量击飞和玩家受伤音效；任意状态都应周期性叫。修法：共享 `MeleeAttack` 拆出 `ResolveHit`，`AttackHitDelay<=0` 时同帧结算伤害/命中音效/击退，非零 delay 仍保留 `timer.Simple` windup 给未来怪物。Zombie 改为 `AttackCooldown=0.75`、`AttackHitDelay=0`、`AttackKnockback=240`、`AttackVerticalKnockback=155`、`AttackGroundedVerticalKnockback=190`；地面玩家受击时先 `SetGroundEntity(NULL)`，再写击退，并在下一 tick 只补缺的 z 速度，绕过 Source ground movement 吃掉竖直击飞的问题。本地 MC 源码确认 `Mob#getAmbientSoundInterval() = 80` tick，Zombie 只返回 `SoundEvents.ZOMBIE_AMBIENT`；BMB 用 20Hz 模拟 `random.nextInt(1000) < ambientSoundTime++` 的递增概率，并把 `MaybePlayIdleSound` 挂到 Base `Think` 可选钩子，避免 debug/held/stranded/长 chase 时停叫。断崖回归修复：共享 `Chase.ApplySafePressure` 统一 `chase_direct` / `attack_ready` / `chase_repath` 的直线压迫，每 tick 对实际 steering target 跑 `IsMovementTargetSafe`，遇到 cliff 发布 `*_cliff` 并压掉水平速度，不再绕过 `path_cliff` 掉下高台。新增 `scripts/check_zombie_phase2_attack_audio.ps1`。
- **Zombie Phase 2 hotfix（代码已切，待用户游戏回归）：近战击退稳定 + MC 方块直追断崖**。用户新测发现近身击退时好时坏，HUD 常见 `dist:0.0`，说明命中时玩家和 Zombie 水平位置重叠导致 `target - mob` 退化；修法是在 chase/attack 过程中缓存最近有效的水平目标方向，`ResolveHit` / `ApplyTargetKnockback` 共用该方向，下一 tick 只补被 Source ground/重叠吞掉的水平/竖直缺口，不重新叠整段击退。另一个问题是 `chase_repath_cliff` 已能拦 Source 地图和 prop 边缘，但 MCSWEP 方块边缘仍可能绕过；修法是在 `IsMovementTargetSafe` 的 Source trace 之后增加 `IsBMBGridMovementTargetSafe`：仅当当前/前方样本处在 MC 方块支撑附近时，按 BMB standable 语义逐半格采样；纯 Source / prop 支撑仍走原 Source trace，不把 prop 顶面写进 A* 支撑。
- **Zombie Phase 2 hotfix 2（历史，已被 hotfix 5 取代）：近战击退多 tick 兜底 + 1 秒攻击间隔 + 轻微屏幕反馈**。用户确认 cliff 已解决，但近身命中仍会出现“坏的时候水平击退和击飞一起消失”，当时尝试了 3 tick correction；后续确认玩家 `SetVelocity` 叠加语义才是击飞不稳主因，correction 已删除。Zombie `AttackCooldown` 改为 `1.0`；实际命中玩家后补轻微 `ViewPunch` + 小幅 `util.ScreenShake`，幅度远低于 HL2 Zombie。
- **Zombie Phase 2 hotfix 3（历史，已被 hotfix 5 取代）：深贴身击退 nudge + 可选日志 + 更明显屏幕反馈**。用户实测 1s 间隔正常，screen shake 还可以再高，击退/击飞仍时有时无；当时尝试 `NudgeTargetForKnockback` + correction retry。后续确认这些会和玩家 movement 抢帧，`SetPos` nudge 与多 tick correction 已删除；日志入口保留并新增 `bmb_melee_knockback_debug 1/0` 作为易用开关。Zombie screen shake / view punch 小幅上调，但仍低于 HL2 Zombie。
- **Zombie Phase 2 hotfix 4（代码已切，待用户游戏回归）：MC 方块平地 `chase_repath_cliff` 误判**。断崖保护在 MCSWEP 方块上新增 BMB standable 采样后，完整方块顶面/边界处可能被 `WorldToBlock` 量化到脚下 solid cell，导致平地也被当作 `not standable`，HUD 显示 `chase_repath_cliff` 并停住。修法：BaseMob 新增 `GetBMBGridFootSample` / `IsBMBGridFootStandable` / `IsBMBGridFootHullClear`，所有直线安全层和 carrot 可见性里的网格 foot 检查都抬高约 `0.12*BS`（至少 4u）后再问 hull/standable；真正离开方块边缘的样本仍会因无支撑返回 `cliff`，但完整 MC 平地不再被顶面边界误伤。
- **Zombie Phase 2 hotfix 5（代码已切，待用户游戏回归）：玩家击飞改为确定速度写入**。4.8 指出关键根因：`Player:SetVelocity` 是叠加，命中帧若玩家带向下残留速度，`+190z` 可能只剩 90z，被 Source ground categorize 吸回地面，导致击飞时好时坏。修法：玩家命中先 `SetGroundEntity(NULL)`，取 `velocityBefore` 后 `SetVelocity(-velocityBefore)` 抵消残留，再一次性 `SetVelocity(direction * horizontal + Vector(0,0,launchVertical))`；删除旧的 3 tick correction 和 `SetPos` nudge，Zombie 上也移除对应参数。新增 `bmb_melee_knockback_debug 1/0` 命令作为更容易输入的日志开关。
- **Zombie Phase 2 hotfix 5b（代码已切，待用户游戏回归）：修单位向量被当成无效方向**。扩展日志显示每次都有 `try=ok` / `resolve=hit`，但没有 `knockback apply`，说明 `ApplyTargetKnockback` 早退。根因是方向已 normalize 后还用 `LengthSqr() <= 1` / `> 1` 判有效，单位向量长度平方正好是 1，被拒掉；缓存方向和 `mob:GetForward()` fallback 也同类。修法：近战击退方向验证改用 `MIN_VALID_DIRECTION_SQR=0.0001`，并在方向无效时打印 `knockback direction_nil/direction_invalid`。
- **Zombie Phase 2 hotfix 5c（代码已切，待用户游戏回归）：发现/攻击距离与踩头命中细调**。用户确认击飞稳定后要求手感收尾：水平击退再降一点、索敌距离扩大、攻击距离略宽、玩家踩在 Zombie 头上也应挨打。修法：Zombie `TargetRange 900→1350`、`TargetLoseRange 1150→1725`、`AttackRange 52→60`、`AttackKnockback 240→210`；正常 `AttackVerticalRange=28` 保持不变，避免高一格平台重新误判成可攻击。共享 `MeleeAttack.IsInRange` 增加可选窄重叠分支：只有目标在上方、竖直差 ≤ `AttackVerticalOverlapRange=86` 且水平距离 ≤ `AttackVerticalOverlapFlatRange=24` 时才允许“踩头”命中。
- **Zombie Phase 2 hotfix 5d（代码已切，待用户游戏回归）：击退距离从 4-5 格收敛到 2-3 格目标**。用户复测发现 `AttackKnockback=210` 配合 `AttackGroundedVerticalKnockback=190` 的滞空时间，合计仍能把玩家推出约 4-5 格，理想是 2-3 格。修法：只降低水平速度，Zombie `AttackKnockback 210→150`；竖直击飞参数保持 155 / grounded 190，避免把刚修稳定的脱地效果重新调坏。注意手动改参数要同步到 GMod addon 目录并重刷实体/刷新 Lua，场上旧实体不一定吃新值。
- **Zombie Phase 2 hotfix 5e（代码已切，待用户游戏回归）：追击中 Zombie 被打不再莫名触发跳跃**。用户反馈 Zombie 站在 MC 方块上追击玩家时，被打后偶发像触发跳跃。根因是 Base 受击击退为了羊的 MC 式上抬，会在 grounded hit 时调用 `loco:Jump()` 打开跳跃态；Zombie 追击状态会立刻回到 chase/hop 管线，MC 方块顶面上容易把这次受击上抬串成一次非预期 jump。修法：Base 新增 `KnockbackUseJump` 开关，默认 true 保持友好生物手感；Zombie 设置 `KnockbackUseJump=false` 且受击竖直速度为 0，只保留水平击退。注意这不影响 Zombie 攻击玩家时的玩家击飞，那是 `MeleeAttack` 的独立玩家 knockback。
- **第四十轮后 hotfix（代码已切，待用户游戏回归）：hop 成功后 `path_hop` / `debug_repath` 来回切**。用户优先复测 hop，发现回滚到最新提交后仍会一格 hop 有动作/顶点正常，但行为层反复 repath。日志复盘：第二十九轮后弹道本体基本未动，第三十八轮为修 final hop 误完成加入 `IsBMBVerticalPathNodeReached`，其中 `WorldToBlock(self:GetPos()).z == node.coord.z` 在方块顶面边界太脆。修法：垂直节点到达不再依赖 cell equality，改为“已落地 + 实际脚底 z 与目标 foot z 在容差内”；未跳上去仍差一整格不会通过，真正落到上层即使 cell floor 到低一格也会被认账。若仍循环，`bmb_debug_hop_log 1` 现在会打印 `hop setup`（ready/reason/face/minFace/speed/safe/hull/steer）、`hop velocity` 和 `vertical reach`，用于确认是不是一格空间退不到理想 face/backoff。
- **第四十轮后 hotfix 2（代码已切，待用户游戏回归）：一格狭窄空间 `face_close` 循环**。用户新日志对比：成功样本 `face≈29 >= minFace≈27.4`，失败样本反复 `face≈20-22 < minFace`，同时 backoff/steer 点 hull 不通，说明不是 apex/弹道问题，而是“想退到理想起跳面距但一格空间退不开”。修法：Base hop launch 新增受保护的 `blocked_close_lift`：只有横向已对准、离台阶不太贴脸（`face >= 0.52*BS`）、目标仍在 hop 范围内，并且理想 backoff 点被 hull/safety 判定不可用时，才允许近距离起跳；普通可退场景仍先 backoff，过贴脸仍拒绝硬跳。`hop setup` 日志新增 `closeMin/backoffBlocked/backoffHull/backoffSafe/backoffReason`；drop 完成改为上下非对称容差，避免已落地但脚底略高于目标 foot z 时误判失败。
- **第四十轮后 hotfix 3（代码已切，待用户游戏回归）：多级严苛台阶里 backoff 点“能站但不能起跳”**。用户确认普通一格方块已能跳；更严苛场景日志显示前几跳成功，卡住那跳从 `face≈16` 退到 `face≈31` 后起跳，但 `apex≈14`，说明理想 backoff 点站立 hull clear，却在两段式 hop 的竖直抬升阶段撞到后方头顶方块。修法：hop launch 新增 `IsBMBHopLaunchCeilingClear`，用 Source `TraceHull` + BMB hull 采样检查起跳点上方约 `0.95*BS` 的净空；`ready` / `close_lift` / `blocked_close_lift` 都必须满足当前位置 lift clear。若理想 backoff 点 lift 不通，则把它视为 `backoffBlocked`，优先改用较近的 `blocked_close` launch 点，日志新增 `currentLift/backoffLift/closeLift` 诊断字段。
- **第四十轮后 hotfix 4（代码已切，待用户游戏回归）：低顶 hop 仍会在 `face_close/lift_blocked` 间来回摆**。最新 `log2` 证明 hotfix 3 已识别 `backoffLift=false`，但有效起跳窗口太窄：实体在 `face≈18.x` 时 `currentLift=true` 但没到 `closeMin≈19.0`，继续走又到 `face≈21.x` 变成 `currentLift=false`，于是转几圈才踩中可跳点。修法：仅当 `backoffLift=false` 时把近距起跳有效门槛降到 `0.48*BS`（日志新增 `effClose`，约 17.5），普通 backoff/hull blocked 场景仍用 `0.52*BS`；同时 hop 垂直到达允许已落地且水平贴近节点时高出目标 foot z 最多约 `1.25*BS` 也算进展，避免 Source 偶发把实体推到目标上方一格后进入 debug_repath 重算。
- **模型动画接入轮（代码已切/已据实测迭代，待用户复测）：sheep 腿摆连续缩放（上提到 base 通用）+ 吃草低头三段动画**。用户已套上第一个 MC 模型（`sheep.mdl`）和走路动画，提出程序化视觉调整，并经游戏内 preview 实测迭代。① **腿摆幅从二元改连续并继续手感调参**：原 `targetSwingAmount = speed > 8 and 1 or 0` 让走/跑都满固定摆幅、只有相位频率随速度变；现 BaseMob 新增通用 `UpdateBMBLimbSwing(speed2D)`，把“速度 → 连续相位 + 连续摆幅强度 [0,1]”这套数学上提到 base（牛/猪可复用），摆幅随速度从静止 0 连续缩放到满速 1（满幅参考速度默认 `RunSpeed`）。本轮 sheep 按实测继续调到 `legSwingMax=25.0`、`LimbSwingMinAmount=0.25`，并把 `LimbSwingPhaseScale` 从默认 0.18 两轮降到 0.09（0.18→0.13→0.09），使走路和跑步腿频率都明显更慢。sheep 删掉自维护的 `BMBSheepLimbSwing*`，改调 helper；摆角和摆轴（roll）仍留 sheep。② **头部 locomotion swing 关闭**：原版羊头不跟身体晃，普通移动不再算 `walkHead/idleHead`，也不每帧锁死 head 骨；退出吃草/preview 后只清一次旧 head pose（`clearSheepHeadPoseOnce`），后续看向系统可接管。注意：此关闭一度“没生效”是 live addon（D 盘）漏同步、游戏读到旧 `walkHead/idleHead` 代码所致，已 robocopy 全量同步修复——改完务必同步整个 `gmod_addon` 到 D 盘。③ **吃草低头三段动画（羊专属，不进 base）**：实测纠正——sheep head 骨骼的**俯仰/低头是 roll 轴（`Angle(0,0,X)`）的负值**（上轮误判为 pitch，已回退），preview 实测够地姿势是 `roll=-55` + `head pos Y=-12`。`eat_grass` 重做成有先后顺序的三段（`duration 1.8`）：①`pos Y` 先下探到 -12（rot 不动）；②`roll` 低到 -55 够地（`EatGrassBiteDelay` 0.42→0.45 对齐咬草那刻）；③`roll` 在 -55↔-40 之间咀嚼循环两回；④`rot` 和 `pos` 一起均匀收回 0。`scripts/check_sequence_animation_adapter.ps1` 同步更新：limb-swing 护栏从 sheep 迁到 base + 新增“禁止二元 `and 1 or 0`”断言，并锁定 `legSwingMax=25.0` / `LimbSwingPhaseScale=0.09`。
- **死亡序列重做（代码已切，待用户游戏回归）：脚本化骨骼倾倒 + 1.9s 停留 + Java poof 粒子**。三部分一起重做 sheep 死亡。① **倒下不再用物理尸体施力**（旧 `prop_physics` + 力，方向随机/不稳）：sheep `UsePhysicsCorpseOnDeath=false`，改纯客户端脚本化倾倒——`UpdateBMBVisualBones` 死亡分支按 `CurTime() - BMBStateStartedAt`（死亡时刻，`SetBMBState` 已网络同步）把 **root 骨骼** 从 0 lerp 到 90°，约 `DeathTipDuration=0.55s`（实测加快）倒完停住；head/leg 随 root 整只翻、自身归零冻住。**关键**：SMD 层级是 `root → body → leg0-3` 且 `head → root`，必须转 root 才整只翻（转 body 会把头留原地）；`CacheBMBSheepBones` 已加 root/body。**侧倒轴实测**：roll(第三分量)=后仰，侧躺改用 **yaw(第二分量)**（`Angle(0, ±tip, 0)`），左右方向用 `EntIndex` 随机固定。② **停留**：`DeathRemoveDelay=1.5`（倒 0.55 + 侧躺约 0.95），无物理尸体分支故 NextBot 不 `SetNoDraw`、客户端持续渲染倾倒，1.5s 后 `Remove`。③ **死亡粒子换 Java poof**：转换器 `mc2source/vtf.py` 新增 `write_animated_bgra8888_vtf`（多帧动画 VTF，单 mip、`numFrames=N`、NOMIP/NOLOD），把 MC 的 `generic_0..7`（8×8、LA）转成 `materials/bmb/particles/mc_poof.vtf` + `mc_poof.vmt`（`UnlitGeneric` 多帧）；`bmb_death_poof` effect 重写为约 20 个粒子，按 `D:\BMBTools\mc26_1_poof_particle_behavior.md` 精确复刻 MC `ExplodeParticle`：逐 tick(20Hz) 模拟 friction 0.9 + gravity -0.1（轻微上飘），出生在包围盒内随机 `- vel*10`，速度 `gaussian*0.02 + 扰动±0.05`(block/tick)，尺寸 `0.1*(rand*rand*6+1)` block（多数小烟点、少数大团），寿命 `16/(rand*0.8+0.2)+2` tick（约 0.9~4.1s，差异大让帧错开），颜色灰白 0.7~1.0，帧按年龄从 generic_7 切到 generic_0，**OPAQUE 无 alpha 渐隐**（消散靠帧 + 到寿命移除）；粒子物理走 20Hz（对齐 MC），渲染按 tick 余量做**帧间外插**避免低 Hz 视觉卡顿；在 `ScheduleBMBDeathCleanup` 移除那刻喷出，身体随之消失。旧 Bedrock 单帧 `mc_white_smoke` effect 被取代。生成脚本 `qs/make_poof_vtf.py` 可复用重转。**本轮补充**：① 死亡后 base `Think` 每 tick 缴械 loco（`SetGravity(0)+SetVelocity(0)+SetDesiredSpeed(0)`），修 flee 跑动 / 跳跃中死亡尸体跟着跑或跳——旧版只在 `StopBMBMovementOnDeath` 清一次不够，loco 醒着会继续 integrate 残留速度（与物理枪 held 抽搐同类）；空中/跳跃中死亡的尸体会静止悬空（`SOLID_NONE` 下靠重力落地会穿地，故选静止），属已知小限制。② poof 早已是 base 能力（`DeathPoofEffect` / `EmitBMBDeathPoofAt` 在 base、effect 全局注册），本轮把 base 默认 `DeathPoofParticleCountMin/Max=18/22`、`DeathPoofRadiusScale=15` 更新为 Java poof 值，sheep 删除重复覆盖（含冗余的 `UsePhysicsCorpseOnDeath=false`，base 已是默认），牛/猪等开箱即用同款 poof。
- **Base 通用 LookAtPlayerGoal + 随机环视（首轮实测：已有 LookAt；已下调频率/Z 幅度并加 idle look-around，待复测）**：LookAt 进 `bmb_base_mob`，分成服务端决定层和客户端渲染层，且与移动并行。看玩家仍只 NW 同步 `BMBLookAtTarget` EntIndex + `BMBLookAtUntil`，不每 tick 同步角度；客户端每帧用玩家已同步的位置自己算方向。服务端每 `LookAtPollInterval=0.5s` 低频检查，没在看目标时才按 `LookAtStartChance=0.06` 随机挑范围内玩家，看 `LookAtDurationMin/Max=2~4s`；超时、目标离开范围、吃草或死亡会清空。没在看玩家时新增随机环视：静止/慢走（默认速度 <= `WalkSpeed+10`）每 `LookAroundIntervalMin/Max=1~3s` 低频挑 `BMBLookAroundYaw/Pitch`（yaw ±60、pitch ±15，`LookAroundForwardChance=0.35` 穿插正前），快跑时清环视回正。客户端 `UpdateBMBLookAtHeadPose(headBone)` 模式顺序：目标 Entity 有效→看玩家；否则环视角有效→环视；否则正前；三种都复用同一套 Lerp。玩家看向 yaw/pitch 分别钳制 `±70/±24`，轴映射沿用 sheep 实测：head rot X 正=向左、负=向右，head rot Z 正=向上、负=向下。Sheep normal visual branch 调用 base helper，吃草/死亡分支仍提前 return，所以不 lookat；普通移动时头不再被 locomotion 锁死。
- **Sheep 声音收尾（用户确认 OK；本轮整体响度继续上调）**：从 `D:\BMBTools\解包音频\minecraft\sounds\mob\sheep` 复制 `say1-3`、`step1-5` 到 addon `sound/bmb/mob/sheep/`，从 `sounds\dig\grass1-4` 复制吃草音到 `sound/bmb/dig/`，并在 `bmb_autorun.lua` 用 `resource.AddFile` 注册；这些 OGG 已统一重采样到 Source 可接受的 `44100 Hz`，避免 `Invalid sample rate (48000)`。Sheep 现在有自己的 `Sounds` 表：`say` 用于 ambient 和受击，`step` 用于脚步，`EatGrass` 用于咬草那刻。脚步不再走 base 的 `NextStepSoundTime` 计时器；sheep 覆盖 `MaybePlayStep()` 为 no-op，客户端 `UpdateBMBVisualBones` 中用和腿摆同源的 `speed * FrameTime()` 累积 `BMBSheepStepDistance`，超过 `StepSoundDistance=35` 播一次并减掉阈值，跑快距离涨得快所以脚步自动变密；脚步音量随速度从 0.50→0.82，ambient say 0.92，受击 say 0.98，吃草 0.82，pitch 88-112 随机。吃草行为 `EatGrass.Try` 改为优先调用 `mob:PlayBMBEatGrassSound()`，基础版先用羊自己的 step OGG，后续接 MC 方块系统的脚下 block step 音。**致死攻击也会叫**：base 在 accepted damage 扣血/死亡判断前调用可选 `OnBMBHurtSound`，sheep 在这里播 say；`OnBMBInjured` 只保留 Flee 逻辑，避免普通受击双叫。
- **物理枪松手后 Wander 不动 hotfix（代码已切，待复测）**：用户发现 physgun 抓起 sheep 再放下后 HUD 回 `wander` 但不走，打一枪/打一下后恢复。根因是 pickup 时 `InterruptBMBMovement()` 留下的 `BMBMoveInterrupt` 没在 drop 清掉，同时 held 期间 desired speed 被置 0、spawn idle 也可能继续拦普通行为；受击会清 interrupt/重启移动，所以看起来“打一下恢复”。修法：`OnBMBPhysgunDrop` 现在 `ClearBMBMovementInterrupt()`，恢复 `MaintainBMBMoveSpeed(WalkSpeed)`，`BMBInitialIdleUntil=0`，再设 `idle` mode/activity；保留原来的向下 -10 速度踹醒。
- **Zombie 模型 + 程序化动画（代码已切，待用户游戏回归）：换转换器模型 + 双足程序化动画**。羊收尾后开僵尸。①**转换器（手臂垂下）**：确认僵尸是双足——`body` bind rotation 为 0（不像羊/牛的 `body -90° stand`），`root` 90°X 是所有 MC 模型通用的 Y-up→Z-up；唯一非零 bind 是手臂，来自 `geo.py` 把 `animation.zombie.attack_bare_hand` 当 rest pose 烘成"前伸标志姿态"。用户要手臂垂下、前伸程序化加（避免在前伸 bind 上叠欧拉旋转踩坑），故 `geo.py` 的 `REST_POSE_ANIMATION_*` 移除 zombie 攻击条目，僵尸家族（zombie/husk/drowned/zombie_pigman/baby_zombie）手臂改垂下中性（`bind_pose_rotation=0`）；skeleton/wither 暂留前伸。`tests/test_mc2source_pipeline.py` 同步（`test_common_hostile...raised_arm` 拆成 skeleton 前伸 + `test_zombie_family_arms_stay_neutral_for_procedural`），62 tests OK；重烘 + 全烘到 addon。②**base 泛化（client 块）**：新增通用关键帧采样器 `BMB.SampleKeyframeAnimation` + `BMB.LerpKeyframeAngle/Vector` + `ENT:SetBMBVisualBoneAngle/Position` + `ENT:ApplyBMBKeyframePose`（羊吃草那套提取上来，羊/僵尸共用），双足 locomotion `ENT:ApplyBMBBipedLocomotion`（两腿反相摆 + 手臂前伸基角 + 反相轻微摆，参数 `BipedLegSwingMax/ArmSwingMax/ArmForwardAngle`）。死亡侧倒/lookat/环视/step/poof/缴械全复用 base。③**zombie 重写**：`Model` 换 `models/mcgm/zombie/zombie.mdl`、删 `RunActivity=ACT_WALK`、`PlayBMBMeleeGesture` 改 `SetNWFloat("BMBAttackStartedAt")`，新增客户端 `UpdateBMBVisualBones`：dead 绕 root 侧倒（yaw，复用羊实测）、normal `UpdateBMBLookAtHeadPose` + `UpdateBMBLimbSwing` + `ApplyBMBBipedLocomotion`、攻击窗内手臂改播前挥关键帧（叠前伸基线、腿继续走）。`check_zombie_phase1.ps1` 护栏从 ACT_WALK 改成程序化双足 + 转换器模型 + 攻击关键帧，禁止回退 Classic.mdl/ACT_WALK/RestartGesture。行为（SeekTarget/Chase/MeleeAttack/声音/受击）不动。**实测点**：双足摆轴默认 roll、手臂前伸 `BipedArmForwardAngle=-60`、攻击关键帧、死亡侧倒 yaw 均为初值，按游戏表现迭代。
- **Zombie 朝向反 180° hotfix（代码已切，已重烘）**：实测双足僵尸整只绕竖直轴反 180°（追玩家只见背面）。根因：转换器 `source.py` 的 `$sequence ... rotate 180` 对所有 entity 硬编码——四足（羊）body 烘了 90° stand，需 +180 yaw 才朝向 Source 正前；双足（僵尸）body 没转，继承这 180 就反了。修法：`make_qc_text` 加 `rotate` 参数，`converter.py` 按 body `bind_pose_rotation` X 是否 >45°（四足 stand）决定 rotate 180/0（羊 90→180 不变、僵尸 0→0）。重烘僵尸（`zombie.qc` 现 `rotate 0`，新 mdl 打包 addon）。`tests/test_mc2source_pipeline.py` 加 zombie QC `rotate 0` golden，62 tests OK。
- **Zombie MC 音效接入（代码已切，待用户游戏回归）**：从 `D:\BMBTools\解包音频\minecraft\sounds\mob\zombie` 复制 `death`、`hurt1-2`、`say1-3`、`step1-5` 到 `sound/bmb/mob/zombie/`，从 `sounds\damage\hit1-3` 复制玩家受伤音到 `sound/bmb/damage/`，并在 `bmb_autorun.lua` 注册；新 OGG 全部用 `D:\oopz\ffmpeg.exe` 重采样到 Source 可接受的 `44100 Hz mono`。Zombie `Sounds` 表已换成模组内 MC 路径：ambient 用 `Say`，受击用 `Hurt`，死亡用 `Death`，近战命中玩家用 `bmb/damage/hit1-3.ogg`。受击音走 base accepted-damage 钩子 `OnBMBHurtSound`，但 Zombie 会先判断这一下是否致死：非致死播 hurt，致死跳过 hurt、只播 death；`OnBMBInjured` 只保留反击锁目标逻辑，避免非致死双播。脚步和 sheep 一样改为客户端距离驱动：`UpdateBMBZombieStepSound(speed)` 在 `UpdateBMBVisualBones` 中累积 `speed * FrameTime()`，`StepSoundDistance=26`（约等于当前双足腿摆半波长），跑/走自动跟腿频同步；Zombie 覆盖 `MaybePlayStep()`，不再走 base 的 0.5s `npc/zombie/foot1.wav` 计时器。`scripts/check_zombie_phase2_attack_audio.ps1` 已加护栏，禁止回退 `npc/zombie/*` / `player/pl_pain*`。
- **第三只怪 Skeleton M1（代码已切，待用户游戏回归——只判对错不判手感/风筝）：核心远程循环 + 模型重烘 + 程序化手臂第一版**。骷髅 = 第二个双足 mob、纯远程弓箭手，spec 在 `H:\工作视频\20251115毕业\BMB骷髅_实现spec.md`。分两条独立风险轨：**轨1 远程逻辑**（占位僵尸模型先验证），**轨2 模型重烘**（双足 rotate 0 + 手臂中性化）。本批两轨都已并入。
  - **新 SENT `bmb_arrow`**：纯 GMod trace 弹丸，`Think` 手动积分重力（`ArrowGravity=320`）+ 沿位移 `TraceLine`，命中世界移除、命中 player/NPC/NextBot `TakeDamageInfo`（`ArrowDamage=6`）；`SetupArrow(owner,dir,speed,spread,damage,gravity)` 设初速 + 散布锥；占位模型 `models/hunter/misc/sphere025x025.mdl`，不碰体素系统。
  - **新行为模块 `BMB.Behaviors.RangedAttack`**：解耦「站哪」和「何时射」。`Update(mob,target)` 每 tick：`UpdateSightMemory`（`mob.BMBSeeTime` 秒累加，trace 眼→眼 LOS）→ `ResolveMovement`（`dist>15格` 或 `SeeTime<1.0s` → chase 走阻塞 `Chase.Run`；否则 aim 停下 + `FaceTarget`，M1 不 strafe）→ aim 时 `UpdateDrawFire`（`NextRangedAttackTime` 冷却 2.0s、满弓 1.0s、`SightLoss<-3.0` 取消）→ `Fire`（MC 抛物线补偿 `d.z += horiz*0.2`，Z-up，spawn `bmb_arrow`）。协程整合：chase 阻塞、aim 一 tick 靠 `RunBehaviour` 的 `coroutine.yield/wait(0.05)` 提供节奏。
  - **`Flee.Run(mob, threat)` 加可选威胁参数**：`pickPanicDestination(mob, threat)` threat 给定时候选点必须比当前离 threat 更远（MC AvoidEntityGoal getPosAway 语义）；不给时保持原 generic panic（羊/僵尸不受影响）。为 M2 逃狼铺路。
  - **新实体 `bmb_skeleton.lua`**：`ENT.Base="bmb_base_mob"`，结构镜像僵尸（`RunBehaviour` 优先级链 dead→held→knockback→debug→stranded→`RunBMBSkeletonAI`）。`RunBMBSkeletonAI` 顺序：① `FindNearestWolfThreat`（6格内类名含 wolf，**现无狼实体返回 nil，逻辑就位不触发**）→ flee；② `SeekTarget.Find`；③ 无目标 → Wander；④ 有目标 → `RangedAttack.Update`。`CanBMBTarget` 仅活玩家；`GetBMBForcedLookTarget` 锁玩家（头追踪）；`OnBMBInjured` 记仇反击；远程常量 `TargetRange=584`(16格)、`RangedAttackRangeCells=15`、`RangedAttackInterval=2.0`、`ArrowSpeed=1168`/`ArrowDamage=6`/`ArrowSpread=3`/`ArrowGravity=320`；双足 `BipedArmForwardAngle=0`（无目标垂下）、`RangedAimArmAngle=-90`（有目标抬弓，**M1 占位双臂抬**，轴/值待实测）；LookAt 沿用僵尸 `LookAtPitchSign=-1/LookAtEyeHeight=64`；MC ambient 概率模型同僵尸。M1 声音用引擎占位（`npc/zombie/*` WAV + shoot 用 `weapons/crossbow/fire1.wav`），MC 骷髅 OGG 由声音收尾批替换。
  - **轨2 转换器**：`qs/mc2source/geo.py` 从 `REST_POSE_ANIMATION_ALIASES/IDS` 移除 `skeleton_attack`/`animation.skeleton.attack`（镜像僵尸家族中性化，**wither_skeleton 仍前伸暂留**）；手臂前伸完全交给 BMB 程序化。`tests/test_mc2source_pipeline.py` 的 `test_skeleton_uses_raised_arm_pose`/`test_skeleton_attack_rest_pose_bind_rotation_is_locked` 改名为 `test_skeleton_arms_stay_neutral_for_procedural`/`test_skeleton_attack_animation_not_baked_into_bind`（断言中性 bind + 空 rest pose），62 tests OK。重烘 `skeleton.mdl` 到 D 盘（`reference.smd` 的 rightArm/leftArm rotation 已为 0）；双足自动 `rotate 0`，`ENT.Model` 已从占位换成 `models/mcgm/skeleton/skeleton.mdl`。
- **Skeleton M1 实测 hotfix 2（用户已验证 2/3/4 通过）：aim 击退 + 索敌范围 + 箭道 + 死亡手臂**。① **aim 状态不受击退（追击却会）**——根因是 aim 把 loco 完全 idle（`SetDesiredSpeed(0)` 不 Approach），落地 nextbot 在 loco 冷态下纯 `SetVelocity` 推不动；追击时 loco 正被 Approach（热态）。修在 **base 击退**：`StartBMBKnockback`/`RunBMBKnockback` 写完 `SetVelocity` 后朝击退方向补 `loco:Approach` 唤醒 loco（用衰减目标，不额外加速；移动中的怪不受影响）。② 索敌 `TargetRange 584→1350`、`TargetLoseRange 730→1725`（同僵尸）。③ **箭还偏高**——常数补偿在近距离过补偿；改成物理精确提前量 `Δz = 0.5·g·horiz²/speed²`（随距离² 自适应），留 `ArrowArcTuning=1.0` 微调。④ 死亡手臂读 `BMBSkeletonArmed`：抬弓死则倒地仍抬着。
- **Skeleton M2 step 1（代码已切，待复测）：strafe 风筝走位**。`RangedAttack.UpdateStrafe`（aim 内每 tick）实现 MC `RangedBowAttackGoal.tick` 横移：`BMBStrafeTime` 每 `StrafeDiceInterval=1.0s` 骰一次（30% 翻顺/逆时针 `BMBStrafeClockwise`、30% 翻前后 `BMBStrafeBackward`）；距离偏置覆盖前后（>13格(√0.75)不退、<7.5格(√0.25)退、中间随机）；横移方向 = 朝目标×前后符号 + 垂直×圆周符号，`loco:Approach(steer, StrafeSpeed=50)` 驱动 + `FaceTarget` 覆盖朝向锁玩家（`TurnRate=540`≈MC 30°/tick）。**安全**：每 tick `Chase.IsSteerTargetSafe` 探 steer 点，不安全先翻圆周方向重试、仍不行则站住只面向（不横进墙/掉崖）。strafe 与拉弓/放箭并行（aim 内同时 `UpdateStrafe`+`UpdateDrawFire`）。opt-out：`RangedStrafe=false` 回到原地 aim。M1 的原地停下 aim 已被 strafe 取代。
  - **M2 剩余**：弓挂手骨（需弓模型素材）、逃狼实测（需狼实体/占位）、左撇子 5%、polish（拉弦手射姿/真箭模型/弓弦 bodygroup/箭插表面）。strafe 速度/换向节奏/转身速率是 §10 游戏内实测项。
- **Skeleton M2 step 2（代码已切，待复测）：strafe 速度微调 + chase 满速 + 准度 + 桩狼 + 弓挂手**。用户复测 strafe 通过。① `StrafeSpeed 50→60`。② **chase 走走停停/速度上不满**：根因是 `RunBMBSkeletonAI` 每次 `RangedAttack.Update` 后的 `coroutine.wait(0.05)`，chase_direct 每 0.28s 撞一次停顿减速；移除该 wait（`RunBehaviour` 每轮已 `coroutine.yield`），chase 段背靠背连续满速、strafe 也更顺；`ChaseSegmentTimeout 0.3→1.0`。③ **箭太偏**：`ArrowSpread 3→1`。④ **桩狼**：新增 `bmb_wolf.lua` 最小占位实体（类名含 wolf、占位盒子模型、物理可抓、无 AI），骷髅 `FindNearestWolfThreat` 能探测到 → 逃狼可测；真狼（跳扑/pack/主动攻击）以后单独做。⑤ **弓挂手**：用户用 BlockBench 导出真 Bedrock bow geo（`D:\BMBTools\BlockBench导出bow\new\B0-B3.geo.json`，B0 idle / B1-B3 逐级拉弓）。转换器新增 `convert_raw_geo`（`mc2source <name> --raw-geo <geo.json> --raw-texture <png>`）：把独立 geo.json + PNG 走与实体相同的 SMD/VTF/VMT/QC/studiomdl/package 烘成单骨 .mdl，跳过 entity 定义/动画；62 tests 仍 OK。已烘 `models/mcgm/bow/bow.mdl`（B0）。骷髅 client 新增 `DrawBMBBow`：`ClientsideModel` 贴 `rightArm` 骨 `GetBoneMatrix`，`LocalToWorld(offset)` 定位，每帧 `DrawModel`；offset/scale 用 convar `bmb_bow_off_x/y/z`、`bmb_bow_ang_p/y/r`、`bmb_bow_scale` 在游戏里拖（贴合手后写回 `ENT.BowAttachPos/Ang/BowScale` 烘死）。死亡不持弓。
  - **M2 剩余 2**：弓 offset 游戏内定值后烘死；拉弓帧（B1-B3 按 `BMBDrawStart` 进度切 bow model，需各烘成 mdl 或 bodygroup）；左撇子 5%（镜像持弓手/抬臂/箭生成偏移）；polish（拉弦手射姿/真箭模型/箭插表面）。
- **Skeleton M2 step 3（代码已切，待复测）：弓左右手 + off-hand 镜像 + 死亡持弓 + 贴脸生成 run-to-face 修复**。① **左右撇子**：出生 5% 掷 `BMBLeftHanded`；主手=右手=模型 `leftArm` 骨，左撇子=左手=`rightArm` 骨；offset 双份烘死：右手 `Vector(-2,-20,-20)/Angle(4,0,0)`，左手 `Vector(-1.5,-18,-23)/Angle(-5,0,0)`（均游戏内实测）。② **off-hand 搭弓**：pitch 按手镜像（右 -35 / 左 35）、roll -90，瞄准时 yaw `math.sin` 在 ±10 缓慢摆（MC 式），全烘死无 convar。③ **死亡持弓**：`DrawBMBBow` 去掉死亡提前隐藏，弓随尸体倾倒、`OnRemove`（poof 那刻）随骷髅消失。④ **贴脸生成 run-to-face**：`ResolveMovement` 接近决策从 `dist>range 或 SeeTime<1s` 改为**只看 `dist>range`**——否则贴脸生成的骷髅会在攒视线那 1s 里追到玩家脸上才转 aim；现在进攻击半径立刻停下 strafe（太近自动后退），SeeTime 只决定何时放箭。
- **Skeleton M2 step 4（代码已切，待复测）：拉弓帧 + 左手 x 微调**。用户确认 step 3 通过。① 左手(5%)`BowAttachPosLeft` x -1.5→**0.5**。② **拉弓帧**：转换器 `convert_raw_geo` 把 B1/B2/B3.geo.json + `bow_pulling_0/1/2.png` 烘成 `models/mcgm/bow_pulling_0/1/2/*.mdl`（弦/箭已烘进各帧贴图，pivot 与 B0 一致故共用同一 offset）。客户端 `GetBMBBowModelPath` 按 `BMBDrawStart`(NW) 拉弓进度切：未拉弓=idle(B0)，进度 <0.65=pulling_0、0.65~0.9=pulling_1、>=0.9=pulling_2（MC 阈值）；`GetBMBBowModelFor` 缓存 4 个 clientside model，`OnRemove` 全清；放箭后 `BMBDrawStart=0` 回 idle。
- **Skeleton M1 实测 hotfix（代码已切，待复测）**：用户确认四点（朝向/手臂垂抬/远程循环/箭命中逻辑）通过，报 5 个 bug，已修：① **受击时手垂下、击退后才抬回**——客户端抬臂原来看 `state=="aim/chase"`，受击时 state 变 knockback 就垂下；改成看 NW bool `BMBSkeletonArmed`（AI 据 TargetEntity 设），与 state 无关，受击/flee 时手保持抬起。② **打他不击退**——根因是 aim 怪击退窗只有 base 默认 0.12s，推力积分 ≈9su 几乎看不见（僵尸靠击退后继续 chase 才显出位移）；骷髅设 `KnockbackDuration=0.35` 让后退明显（水平击退仍走 base，竖直 0）。③ **箭全飘太高**——`ArrowGravity` 320→**730**（MC 实际 0.05 block/tick²），抛物线补偿系数提为可调 `ArrowArcCompensation=0.15`（MC 0.2 含空气阻力，无阻力近似 0.15），打高大幅改善。④ **箭从裆部射出**——nextbot `EyePos` 偏低，改用显式 `RangedSpawnHeight=50` + `RangedSpawnForward=16` 算 spawn。⑤ **wander 时靠近要等很久才打**——`WanderPause` 之前 4~10s（羊节奏），wander 阻塞在 `InterruptibleWait` 不重扫目标；改 0.5~1.5s（对齐僵尸），靠近后 1~3s 内转入攻击。箭速/补偿/spawn 高度/瞄准臂轴仍是游戏内实测项。
- **Skeleton M2 polish（代码已切，待复测）：真箭模型 + 瞄下半身躯干（修某些角度射不中）+ 远程诊断日志**。用户报骷髅瞄准位置像头部、某些角度站着不动也射不中人，并已用 BlockBench 导出真箭。① **占位小球→真箭模型**：`bmb_arrow` 的 `ENT.Model` 从 `models/hunter/misc/sphere025x025.mdl` 换成转换器 `convert_raw_geo` 烘的 `models/mcgm/arrow/arrow.mdl`（已打包到 D addon，源仓库不跟踪 models）；新增 `ENT.ArrowModelAngleOffset=Angle(0,0,0)` 朝向修正，`SetupArrow` 和 `Think` 飞行分支都 `SetAngles(vel:Angle() + (ArrowModelAngleOffset or angle_zero))`（箭头不沿飞行方向时分量加这个微调；vel:Angle() roll 恒 0）。② **瞄眼睛→瞄下半身躯干**：根因是 `RangedAttack.Fire` 原来瞄 `rangedEyePos(target)`（眼/头高度），小幅抛物线弧线误差会从头顶掠过打不中；改成瞄目标身高 1/3 处（`aimZ = OBBMaxs().z * RangedAimHeightFrac`，`aimPos = GetPos()+Vector(0,0,aimZ)`），对齐 MC `performRangedAttack` 的 `getY(0.3333)`；骷髅新增可调 `RangedAimHeightFrac=0.3333`（打高调小、打低调大）。③ **远程诊断日志**（用户要求补日志）：新增 convar `bmb_debug_ranged` + 控制台 `bmb_ranged_debug 1/0`（管理员，镜像 melee knockback 日志那套）；`RangedAttack.Fire` 打印 `horiz/vGap/arc+/launchPitch/spd/g/t/predZerr`，其中 **predZerr=预测落点 z − 瞄准点 z**，≈0 说明弹道正确（脱靶=散布/目标移动）、偏大=打高、偏小=打低；`bmb_arrow` 命中打印命中对象/位置/飞行时间，超时打印 no-hit 落点。已 lint（C+D 各一遍，exit=0）并把 `bmb_arrow.lua`/`sv_behaviors.lua`/`bmb_skeleton.lua` 三个文件**增量**同步到 D addon（不镜像，D 的 40 个 models 文件不在源仓库）。**复测**：开 `bmb_ranged_debug 1`，确认箭是真箭模型、命中身体（不再从头顶掠过）、日志 `predZerr≈0`；箭头朝向不对就调 `ArrowModelAngleOffset` 分量；瞄高/低再调 `RangedAimHeightFrac`。**用户已实测：打不中已修 ✅；箭模型横置 → `ArrowModelAngleOffset` 实测定为 `Angle(0,-90,0)`（yaw 转正对齐飞行方向）。** MC 音效已接入（替换 M1 占位的 zombie WAV + crossbow）：从 `D:\BMBTools\解包音频\minecraft\sounds\mob\skeleton`（say1-3 / hurt1-4 / death / step1-4）+ `random/bow.ogg` 复制并用 ffmpeg 重采样 44100Hz mono 到 `sound/bmb/mob/skeleton/`（bow 归此目录命名 `bow.ogg`），`bmb_autorun.lua` 注册 13 个文件。骷髅 `Sounds`：Idle=say（ambient 概率模型）、Hurt=hurt1-4（受击钩子 `OnBMBInjured`）、Death=death（致死 `OnKilled`）、Shoot=bow（`PlayBMBRangedShootSound` 放箭那刻）、Step=step1-4（**客户端距离驱动**——新增 `UpdateBMBSkeletonStepSound` 镜像僵尸双足、覆盖 `MaybePlayStep` no-op、`StepSoundDistance=26`）。已 lint（C+D exit=0）、同步 3 lua + 13 ogg 到 D。**复测**：ambient 偶尔骷髅叫、受击/致死音正确、放箭有 MC 弓声、走/跑脚步随速度变密、箭头朝飞行方向；**至此骷髅核心基本完成**，剩 polish（拉弦手射姿 / 真箭插表面 / 左撇子细节）。
- **Skeleton polish（代码已切，待复测）：真箭插表面 + bowhit 命中音**。用户确认拉弦手射姿不做、左撇子已完成。本轮两件：① **真箭插表面**（`bmb_arrow`）：命中分两类——命中生物（player/NPC/NextBot/IsBMBMob）仍扣血 + bowhit + 移除（不插）；命中世界/静态物体改为**插着不移除**：置 `BMBStuck`（Think 跳过弹道积分、只等 despawn）、位置摆到命中点 + 沿速度方向推入 `ArrowEmbedDepth=8`（箭头埋进、杆翎露出）、角度保持命中那刻（已对着表面）、`SOLID_NONE` 人可穿过、`ArrowStuckLifetime=15s` 后移除防插一地箭堆爆。会动的 prop **不 SetParent 跟随**（静态版覆盖几乎所有情况，按用户要求跳过；prop 移动后箭悬空是已知小瑕疵）。② **bowhit 命中音**（MC `random/bowhit1-4`，ffmpeg 重采样 44100Hz mono → `sound/bmb/random/`，`bmb_autorun.lua` 注册 4 个）：命中玩家受击 + 插静态表面都随机播一个（`bmb_arrow.HitSounds` + `PlayBMBArrowHitSound`，level 75 / vol 0.85 / pitch 94-106）。已 lint（C+D exit=0）、同步 `bmb_arrow.lua` / `bmb_autorun.lua` + 4 ogg 到 D。**复测**：箭射墙/地/MC 方块插着约 15s 后消失、人能穿过、箭头朝表面（埋入深浅调 `ArrowEmbedDepth`）；命中玩家和插表面都有 bowhit 噗声。**骷髅至此核心 + 表现基本完成**（剩可选 polish：箭插表面再贴合、prop 跟随）。
- **参数微调（代码已切，待复测）**：① 用户实测真箭埋太深 → `bmb_arrow` `ArrowEmbedDepth` 8→**2**（箭头只埋一点，杆翎更露在外）。② 用户觉得僵尸偏快 → `bmb_zombie` `WalkSpeed` 92→**65**、`RunSpeed` 95→**70**；并把 `AttackMoveSpeed` 92→**70** 对齐 RunSpeed（原设计 attack≈run，否则基础速度降了、一进攻击仍会窜出去）。已 lint（C+D exit=0）同步到 D。
- **复杂方块寻路 + 路径缓存 TTL 桩（代码已切，待用户游戏回归）：接 MCSWEP 形状查询**。朋友给了形状查询接口（`interface-usage`），本轮按"务实增量"接进 `RealBlockWorld` 适配层（所有 MC.* 仍收在 adapter，CLAUDE.md 铁律）。① **形状接入**：新增 `cellProvidesStandableTop`——full cube 快路径 `MC.BlockIsFullCube`、窄连接碰撞（玻璃板/栅栏/墙/栏杆/管，读 `MC.Blocks[id].connection`）排除、否则 `MC.GetCellBlockExtent`（带坐标、邻居已解）有顶面包络 `hi` 即可站（半砖/台阶/雪层/楼梯/斜坡都走这条，斜坡按 `extentHi` 近似）。`real.HasSupport` 的脚下判定从"只认 full cube"（`real.IsSolid`）换成 `cellProvidesStandableTop`，于是半砖/台阶/楼梯/斜坡作脚下支撑被识别（mob 能站上去），≤半格高差靠 loco StepHeight(28>半砖18.25) 走上、整格仍 hop；玻璃板/栅栏/墙顶不再被规划为落脚点。A* `neighbors`/`isStandable`/`real.IsSolid`/mock 都**不动**（半砖经 HasSupport 自然变成同层 walk 邻居）。② **路径缓存 TTL 桩**（接口需求第三条"失效"，MCSWEP 还没做、先做光照）：`sv_pathfinder.lua` 新增集中单点 `pathfinder.ShouldRepath(waypoints)`，`FindPath` 给 waypoints 打 `bornAt`（完整 + partial 两条出口），缓存超过 `BMB.Config.PathCacheTTL=0.75s` 就重寻路（世界变了路径 ≤0.75s 自愈、永不走死路）。**明确临时桩**：等 `MC.GetChunkVersion` 出了，改成给路径标记/比对经过 chunk 的版本号、替换进同一个 ShouldRepath（现在不搭 chunk 版本标记）。接入点 `bmb_base_mob.lua` 的 `MoveAlongPath` 主循环安全点（非 hop/drop、无进行中 `BMBActiveBlockHop`），过期走既有失败出口让 Chase/Wander 重算，绝不在垂直动作中途打断。新增 `scripts/check_block_shape_pathing.ps1` 护栏（形状调用只在 adapter、ShouldRepath 是 TTL 桩且标了 GetChunkVersion 替换点、窄碰撞排除）。**复测**：放半砖/台阶/楼梯/雪层 → mob 能站上/走过（不再当空气或误判 cliff），斜坡能站；玻璃板/栅栏/墙不当落脚点；破坏路径中间一格方块 → ≤0.75s 重规划绕行；`bmb_world mock` 与已验证的羊/僵尸平地/整格 hop/drop/走廊寻路不回归。
- **复杂方块寻路 hotfix（代码已切，待用户游戏回归）：chase cliff 在方块走廊/台阶 churn 成 vel:0 修复**。用户实测楼梯能识别 ✅，但僵尸在方块走廊里卡 `chase_repath_cliff` + `vel:0.0`。根因两条（用户诊断）：① cliff 检测吃同一份表面数据，半格/整格下台阶（drop 容差内）被整数格 standable 当成悬崖；② 无迟滞——某帧前方安全探测偶发"无支撑"（探针打侧墙、MCSWEP 方块 chunk 边界碰撞重建脏读、整数格量化），立刻翻 cliff 压速、下帧又好，path↔cliff 反复横跳把 vel churn 成 0。修法：① **drop 容差**——`IsBMBGridMovementTargetSafe` 采样无支撑时先调新增 `IsBMBGridFootDroppable`（下方 ≤MaxPathDropCells 有可站面 = 可下台阶），只有下方一路空才真 cliff（与 A* drop 规则一致）；② **cliff 迟滞**——共享 `Chase.ApplySafePressure` 连续 cliff 持续超过 `CliffHysteresisTime=0.12s` 才真压速，瞬时 cliff 容忍、继续前压（`BMBCliffSince` 计时，安全/wall 时清零）；wall（hull 实撞）不迟滞。一处覆盖 `chase_direct`/`attack_ready`/`chase_repath` 全部 chase cliff。护栏 `check_block_shape_pathing.ps1` 加迟滞/drop 容差断言。**复测**：方块走廊/台阶里僵尸持续前压、不再 `*_cliff` + vel:0 卡死；真悬崖（下方 >3 格空）仍停边缘；下 ≤3 格台阶顺畅；平地不再因单帧脏读抖动。
- **复杂方块寻路 hotfix 2（代码已切，待用户游戏回归）：撤销 drop 容差 + 加 chase 诊断日志**。① **撤销 drop 容差**：上一条加的 grid `IsBMBGridFootDroppable`（下方 ≤3 格有可站面就不算 cliff）有副作用——`chase_direct`（看见玩家直压）把整格下台阶当可走，僵尸站台阶上、旁边明明有路，却反复从边缘直冲跳下、够不到上方/旁边的玩家（死循环）。改回：`IsBMBGridMovementTargetSafe` 采样无支撑即 cliff（删 `IsBMBGridFootDroppable`）；整格下台阶交给 A* 决定（玩家在下→A* drop 追、在上→hop 上、旁边有路→绕），而不是 chase_direct 直冲下。半砖/台阶半格高差本就由 `cellProvidesStandableTop`(standable)+loco StepHeight 处理，不依赖 drop 容差。**保留 cliff 迟滞**（平地单帧脏读 churn 仍治）。② **chase 诊断日志**：另一处问题——僵尸卡 `chase_repath_cliff` + `vel:0` + `node:0`（A* 产不出可走路径，"旁边有路 A* 没找到"，疑似搜索预算椭圆界 `f≤hStart*2+24` 把绕路剪掉，或玩家所在格 A* 判不可达）。这是 A* 核心，盲改预算有性能/回归风险，先按"overlay/log 分清"加 convar `bmb_debug_chase` / 控制台 `bmb_chase_debug 1`：`MoveToWorldPosition` 打印 `findpath dist=N -> nil/partial(N)/full(N)`，分清钉死是 A* 无路、partial、还是后续 MoveAlongPath 被拦，下一轮按日志精确修。护栏 `check_block_shape_pathing.ps1` 去 droppable 断言、加"droppable 已撤销"反向断言。**复测**：僵尸站台阶上、旁边有路应走 A* 绕/爬过去，不再反复跳下；若仍卡 `chase_repath_cliff` + node:0，开 `bmb_chase_debug 1` 复测把日志发来。
- **debug tool hotfix（代码已切，待用户游戏回归）：右键羊上台阶不去 → 目标 snap 到可站格**。用户报 debug tool 右键羊去台阶上方它不上（`state:debug_move mode:debug_repath node:0 adv:0` 卡住），但同地形僵尸 chase 能 hop 上。根因：debug 目标用 `targetFromTrace` 粗处理点击点（顶面 +4u / 侧面推半格），没 snap 到合法可站格；点到台阶边缘/斜面/不可站处时 A* 的 goal 不 passable → `FindPath` 直接 nil，而 debug 目标是固定点、每次 repath 都 nil → 永远 `debug_repath` 不动（chase 用玩家可站位置且会移动，所以同地形能上）。修法：`bmb_debug.lua` 的 `startTargetMove` 先 `BMB.Pathfinder.IsStandablePosition(target,{mob})` 判目标格本身可站、可站用格中心；否则 `FindNearestStandable`（radius 4）snap 到最近可站格中心。这样 A* goal 必合法可达。护栏 `check_block_shape_pathing.ps1` 加 snap 断言。**复测**：右键台阶上/各种面，羊应像僵尸一样寻路（hop）过去，不再卡 debug_repath。
- **debug tool hotfix 2（代码已切，待用户游戏回归）：右键上台阶真根因——carrot 跨 hop 节点被直线 grid 可见性缩回原地**。上一条 snap 目标后用户实测仍卡，开 `bmb_chase_debug` 拿到关键证据：`findpath dist -> full(7)` **反复刷屏**——A* 已给**完整路径**，但 `MoveAlongPath` 走不动（`node:0 adv:0 vel:0`）。真根因：`GetPathPointAhead` 的 carrot pure-pursuit **跨过即将到来的 hop 节点**投到台阶上方，`GetPathCarrot` 的 `IsPathGridVisible` 直线检查穿台阶面/上升处（中间格 not standable）判不可见 → 二分把 carrot 缩回原地 → mob 朝原地"走"= 不动 → no-progress/ShouldRepath fail → `debug_repath` → 重新 FindPath 又 full(7) → 死循环（chase 同病；wander 路径短少跨 hop，所以 wander 能上台阶而 debug 远目标不能）。修法：`GetPathPointAhead` 前推遇到 hop/drop 垂直节点就**停在该节点的水平投影**（不跨过），carrot 落到台阶根上方、直线可见，mob 走到台阶根 → 推进 → 由 hop 流程逐级上。上一条目标 snap 保留（让 A* goal 合法，full(7) 正是它生效）。护栏 `check_block_shape_pathing.ps1` 加 `carrot-stops-at-vertical-node` 断言。**复测**：debug 右键台阶上方，羊应走到台阶根逐级 hop 上去，不再卡 `debug_repath`；wander/chase 上台阶应一并更顺；平地/拐角 carrot 不受影响（只垂直节点处改变）。
- **新生物 Husk + Stray（代码已切，待用户游戏回归）：ENT.Base 继承式快速实现**。用户要两个逻辑相同的新生物：Husk（沙漠僵尸=近战，逻辑同 Zombie）、Stray（雪地骷髅=远程弓箭手，逻辑同 Skeleton）。① **烤模型**：`python -m mc2source husk` / `stray`（samples 有各自 entity.json+geo+png，`DEFAULT_SAMPLES_ROOT` 已指 `D:\BMBTools\bedrock-samples-v1.26.20.4-full`；husk 属僵尸家族手臂已中性化、stray 走 skeleton 家族），各 packaged 到 D addon（`models/mcgm/husk`、`models/mcgm/stray` + 材质）。② **行为零重复**：`bmb_husk.lua` `ENT.Base="bmb_zombie"`、`bmb_stray.lua` `ENT.Base="bmb_skeleton"`，只覆盖 Model/PrintName/Spawnable/Category——近战(SeekTarget/Chase/MeleeAttack) 或 远程(RangedAttack/弓/strafe)、客户端双足动画/攻击/抬弓/死亡侧倒、ambient/受击/脚步/弓声、全部参数都继承（zombie/skeleton 无硬编码类名，继承安全）。③ **音效零新增**（用户要求用别的音效）：husk 不写 `Sounds` 即自动继承 zombie 音（`bmb/mob/zombie/*`），stray 继承 skeleton 音（`bmb/mob/skeleton/*` + bow），箭命中音走 bmb_arrow 的 `bmb/random/bowhit*`——都已在 `bmb_autorun` 注册、文件已存在，无需复制。GMod 自动扫 `lua/entities/` 加载 SENT、`Spawnable=true` 自动进 spawn menu（Entities > BlockMob Base），无需改 autorun。lint C+D 通过。**复测**：spawn `BMB Husk`（沙漠僵尸，近战/受击/死亡/音效同 Zombie）、`BMB Stray`（雪地骷髅，弓箭/strafe/拉弓/死亡/音效同 Skeleton）。已知可选 polish：stray 的 `stray_overlay`（破布外套层）未合成、先用基础贴图；husk/stray 手骨 offset 沿用 zombie/skeleton 实测值，弓/手臂位置略偏再各自微调。
- **Husk/Stray 修正（代码已切，待用户游戏回归）：Stray 隐身/递归报错 + 独立音效 + 外套合成**。用户指出不能只继承原音效、Stray 完全隐身且 `bmb_skeleton.lua:366 Draw` stack overflow。① **Draw 递归**：`bmb_skeleton:Draw()` 不再用 `self.BaseClass.Draw(self)`（Stray 继承 Skeleton 时会解析回 Skeleton 自己），改为显式调用 `scripted_ents.GetStored("bmb_base_mob").t.Draw(self)`，再画弓。② **独立音效**：从 `D:\BMBTools\解包音频\minecraft\sounds\mob\husk` / `stray` 复制并重采样 44100Hz mono 到 `sound/bmb/mob/husk|stray/`；Husk `Sounds` 覆盖 `idle1-3/hurt1-2/death1-2/step1-5`，Stray 覆盖 `idle1-4/hurt1-4/death1-2/step1-4`，Stray 射箭仍用通用 `bmb/mob/skeleton/bow.ogg`，箭命中仍用 `bmb/random/bowhit*`；`bmb_autorun` 已注册。③ **Stray 外套**：转换器 `mc2source` 对 `stray` 专门加载 `geometry.overlay`（`geometry.stray.armor`）并把 overlay cubes 合进同名骨骼，同时把 `Texture.default + Texture.overlay` alpha composite 成一张 VTF，不再需要运行时双 render controller；新增测试 `test_stray_composes_overlay_geometry_and_texture`，qs 63 tests OK；已 `.\mc2source.cmd stray` 重烘并 packaged 到 D addon。**复测**：spawn Stray 应可见且无 stack overflow，身上有破布外套层；Husk/Stray 用各自 idle/hurt/death/step 音效；行为仍分别继承 Zombie/Skeleton。
- **新生物 Parched（代码已切，待用户游戏回归）：同 Stray 的 Skeleton-family 导入**。用户要一个跟 Stray/骷髅一样的 `Parched`。① **模型**：Bedrock samples 里 `parched.entity.json` 是单层 `geometry.parched` + `textures/entity/parched/parched.png`，没有 Stray 那种 overlay render controller；已 `.\mc2source.cmd parched` 重烘并 packaged 到 D addon（`models/mcgm/parched/parched.mdl`，转换日志 `triangles:156`、texture transparency true）。② **实体**：新增 `bmb_parched.lua`，`ENT.Base="bmb_skeleton"`，只换 `Model/PrintName/Spawnable`；弓箭、strafe、拉弓帧、死亡、lookat、脚步距离驱动都继承 Skeleton。③ **音效**：从 `D:\BMBTools\解包音频\minecraft\sounds\mob\parched` 复制并重采样 44100Hz mono 到 `sound/bmb/mob/parched/`；`bmb_skeleton` 的 class-based `GetBMBSkeletonSounds()` 新增 `bmb_parched` 表（ambient1-4/hurt1-4/death/step1-4，射箭仍用 skeleton bow），避免继承链串回 skeleton/stray 音效；`bmb_autorun` 已注册。④ **菜单**：`mcgm_autorun` 加入 NPC 列表 `BMB Parched`。已同步 live addon，glualint 通过。**复测**：spawn Parched 可见、远程弓箭行为同 Stray/Skeleton、用 Parched 自己的 ambient/hurt/death/step 音效。
- **敌对 chase direct/repath cliff memory（代码已切，待用户游戏回归）**。用户反馈复杂绕路时敌对怪会每走一小段就丢开 A* 试直扑玩家，发现同一条直线是悬崖后切回路径，过会儿又重复同一个 `chase_direct_cliff` / `chase_repath_cliff`。结论不是 cliff 检测错、也不是 A* 不会绕路，而是直线前压没有足够失败记忆。修法很窄：共享 `Chase` 在 `ApplySafePressure(..., "chase_direct"|"chase_repath")` 经 cliff 迟滞确认失败后写 `BMBChaseDirectCliffBlock`（目标 EntIndex、当时 mob/target 位置、冷却/过期）；`CanDirect` 和 `TryRepathPressure` 先查缓存，冷却内或双方没移动超过阈值就不再测同一条直线，老实走既有 BMB A*。第二轮实测发现僵尸已走 `TryRepathPressure`，但旧的 1.2s/2 格门槛太小，A* 绕路本身走一小段就清掉记忆，导致重新进入 `chase_repath_cliff`；现默认调为 `ChaseDirectCliffMemoryCooldown=3.0s`、`Duration=25s`（动态方块世界兜底重试）、mob 移动阈值 6 格、目标移动阈值 2 格，并暴露 `bmb_chase_cliff_memory_*` / `bmb_chase_repath_cliff_giveup_time` convar 供真图调参。若 A* 永远失败且直线记忆持续阻塞，`ChaseRepathCliffBlockedGiveUpTime=4s` 后短暂放弃目标回 wander/重扫，避免钉在崖边。`attack_ready` 不写这个缓存，避免贴边近战被过度抑制；A*、形状查询、悬崖判断本身不动。护栏 `check_block_shape_pathing.ps1` 已加 direct/repath cliff memory 断言。**复测**：同一复杂绕路/悬崖场景里，敌对怪第一次直追确认 cliff 后应继续沿 A* 绕，不再每 0.28s 或每小段路重复撞同一条死直线；mob 或目标明显换位后才允许重新试直追，世界变化最迟约 25s 兜底重测。
- **Zombie chase_direct 距离收紧（代码已切，待用户游戏回归）**。用户实测 cliff 已明显改善但远距离仍太早进 `chase_direct`。共享 `Chase.CanDirect` 增加可选 `ChaseDirectMaxDistance` / `ChaseDirectMaxDistanceCells` 上限；Zombie 家族先设 `ChaseDirectMaxDistanceCells=6`，远处继续走 A*，靠近后才切 direct。Skeleton/Stray/Parched 不改，避免影响刚测通过的 Parched。
- **通用受击还击目标（代码已切，待用户游戏回归）**。骷髅箭已经用 shooter 做 `DamageInfo` attacker（不是箭本体），本轮补上 base 级目标重选：`bmb_base_mob` 新增 `IsBMBCombatTarget` / `TryBMBRetaliate`，非致死受击后读取 `damageInfo:GetAttacker()`，通过 `CanBMBTarget` 后写入共享 `TargetEntity` 并清 `NextTargetScanTime`，优先级盖过默认玩家，直到目标死亡/失效/超出 lose range。Zombie/Skeleton 的 `CanBMBTarget` 改用通用战斗目标（玩家、NPC、NextBot、BMB mob），forced look 也跟随任意当前战斗目标；Husk/Stray 删除玩家-only 的本地受击改目标代码，统一走 base。`RetaliateSameClass=true` 默认允许同类流弹互殴，未来要 MC 式压掉同类可单行设 false。新增 `scripts/check_retaliation_targets.ps1` 护栏。**复测**：Skeleton 箭误伤 Zombie → Zombie 放弃玩家优先追/咬 Skeleton；Skeleton 被 Zombie 打中 → Skeleton 改射 Zombie；Stray/Parched/Husk 同理继承；羊仍只按自己的 Flee 逻辑逃跑，不会反打。
- **摆拍辅助：notarget + bmb_freeze（代码已切，待用户游戏回归）**。Lua NextBot 不走 Source NPC 的 `notarget` / `ai_disabled` 管线，本轮补 BMB 自己的入口：`SeekTarget.IsValid` 和 base `IsBMBCombatTarget` 都会跳过 `player:IsFlagSet(FL_NOTARGET)` 的玩家，所以控制台 `notarget` 后敌对怪会丢失/不再锁该玩家。新增服务器 convar `bmb_freeze 1/0`：base Think 每 tick 中断移动、清 lookat、速度归零并发布 `state/mode=frozen`；Sheep/Zombie/Skeleton 的 `RunBehaviour` 顶部也停住行为协程，继承类自动生效。用途：摆好 NPC → `bmb_freeze 1` → noclip/notarget 拍图标；拍完 `bmb_freeze 0` 恢复。新增 `scripts/check_notarget_freeze.ps1` 护栏。
- **MCSWEP 光影兼容（代码已切，待用户游戏回归）**。朋友的光影 addon 通过客户端 `mc_light_enable 1/0` 控制 `mc_light_enabled`，方块 mesh 在 `cl_render.lua` 里用 `vertexcolor` + `MC.SampleLighting(bx,by,bz)` 得到 brightness。BMB mob 是模型，不能自动吃方块 vertex color；本轮在 base client `Draw` 增加 `GetBMBMCLightBrightness()`：若客户端存在 `MC.WorldToCell`/`MC.SampleLighting`，按 mob 身体中部采样当前格 brightness，并用 `DrawBMBModelWithMCLight()` 临时 `render.SetColorModulation(brightness)` 绘制模型；`mc_light_enable 0` 或无 MC 光影时 brightness=1，外观不变。受击/死亡红闪也乘同一 brightness；Skeleton-family 手持弓改用同一 helper，避免暗处弓还发亮。新增 `scripts/check_mc_lighting_compat.ps1` 护栏。**复测**：开 `mc_light_enable 1`，洞穴/阴影中 Zombie/Skeleton/Sheep/Parched 等模型应和周围 MC 方块一起变暗；关 `mc_light_enable 0` 恢复原亮度。
- **Wolf Phase 0（代码已切，待用户游戏回归）**。`H:\工作视频\20251115毕业\wolf_plan.md` 拆狼为 Phase 0-4；当前只做 Phase 0 基础狼，不做 taming、不做猎杀/pack/leap。旧 `bmb_wolf` stub 方块替换为真正 `bmb_base_mob` NextBot：`models/mcgm/wolf/wolf.mdl`、薄调度链（freeze/held/knockback/debug/stranded/initial idle/wander）、方块 A* Wander、客户端四足骨骼（`head/body/upperBody/leg0-3/tail`）+ Base LookAt + Base limb swing + death side-tip，`MaybePlayStep` 暂时空实现避免 base zombie placeholder 脚步。`mcgm_autorun.lua` 已注册 `BMB Wolf` NPC，类名保留 `bmb_wolf`，Skeleton 预留的 `FindNearestWolfThreat()` 会自动把它当狼逃。转换器 hotfix：上一版把 `wolf_setup -> animation.wolf.setup` 当通用 rest pose，连 position 一起烘进 bind pose，导致 body/legs 下陷、head 留在上方；现改为 wolf 专属 `structural.wolf_body`，只给 body/upperBody 烘 `(90,0,0)` 四足旋转，leg/tail/body/upperBody 的 `bind_pose_position` 保持 0。新增转换器测试锁定“不烘 setup position”，`python -m unittest discover -s tests` 64 tests OK；`.\mc2source.cmd wolf` 已重新编译并打包到 D 盘 live addon。新增 `scripts/check_wolf_phase0.ps1` 护栏，防旧 stub 回归并锁住 Phase 0 不混入 combat/pack/leap。本轮姿势微调：腿已复测正确，不再改 converter；wolf 渲染层新增 `body/upperBody/tail` client pose convar（`bmb_wolf_<part>_rot_*` / `_pos_*`），实测默认 `bmb_wolf_tail_rot_z=-45`、`bmb_wolf_tail_pos_z=0`、`bmb_wolf_upper_body_pos_z=-10`；尾巴 walk/run 只在 `rot_x` 轴自然摆 `-40..40`，idle 保持 `rot_x=0`，不做 `rot_z` 上下摆。**复测**：spawn `BMB Wolf` 应可见、不是方块 stub，身体/腿不再下陷，能 idle→wander 走方块 A*，debug tool 可移动，`bmb_freeze` 可定住，Skeleton 应继续主动躲它；尾巴 idle 为 `rot_z=-45` 下垂，移动时 `rot_x` 自然左右摆。
- 本轮修掉的对接隐藏 bug（接 real 之前就存在）：
  1. **mock 占死 `BMB.BlockWorld` 名字、无切换机制** → mock 改名 `BMB.MockBlockWorld`；新增 `BMB.SelectBlockWorld()` + convar `bmb_use_real_world`（默认 1，MCSWEP 不在场回退 mock）+ 控制台 `bmb_world mock|real`。MCSWEP 比 BMB 后加载（addons 字母序），所以 `BaseInitialize` 生成 mob 时会再选一次（幂等）。
  2. **类型枚举对不上**：real `GetBlockAt` 原来返回数字 id，行为层比较的是 `BMB.BlockTypes.Grass` 字符串，永远不相等 → adapter 现在做 id↔枚举双向映射（`blockTypeToId`/`idToBlockType`），未建模的 id 原样透传。
  3. **吃草坐标差一格**：real 世界里 `WorldToBlock(GetPos())` 是脚部所在的**空气格**，不是脚下的草方块 → EatGrass 改为 `GetPos() - Vector(0,0,4)` 再换算（mock 忽略 z，行为不变）；并把 mob 作为 actor 传入 `SetBlockAt`（real 转成 MCSWEP 的 `{actor=ent}`，带进 OnPlace/OnBreak 和声音粒子）。
  4. **寻路没查头部格**：A* 新增 `isPassable` = 脚部格 + 头部格都非实心（mock z=1 恒空，行为不变）。
  5. **`MaxStepDown` 34 < 36**：站在一格高方块地板上会把"走下来"判成悬崖永远不下来 → 先改 40；第二十四轮参数化为 `1.1 * BMB.BS`（36.5 时 40.15，>1 格且 <2 格）。
- real adapter 其余实现：`EnsureInitialized` no-op；`GetRandomWalkablePoint` 在脚部层随机选"脚+头双空"的格子（不要求脚下有 MC 方块，flatgrass 地皮也算地）；`IsSolid` = `GetBlock`→`GetBlockOrient`→`BlockIsFullCube` 粗略版（半砖/楼梯/栅栏当可通过，实际碰撞由移动层 Source 探测兜住，细化入口 `MC.BlockBoxes`）；写入 `MC.SV.SetBlock`，失败打日志（`unchanged` 不算错）。
- CLAUDE.md 已同步更新（接口文档指向 mcswep-main/docs；"已知缺口"段落改为"仍然禁止"两条：Place/Break 玩家专用、SetBlockRaw 禁用）。

## 协作流程（固定，每次改完都做）

1. 改完代码跑 `H:\工作视频\20251115毕业\glualint.exe lint <改过的文件>`。
2. **同步整个 `gmod_addon/` 到 `D:\SteamLibrary\steamapps\common\GarrysMod\garrysmod\addons\gmod_addon\`**（用户在游戏里直接测）。
3. **更新本文件 + `.planning/mcgm-main/` 四个文件**（task_plan / progress / findings / status_summary，codex 接手要看）。

## 复测清单（当前：Zombie 模型 + 程序化动画）

0c. **Zombie 模型 + 程序化动画必测（本轮重点）**：生成 `BMB Zombie`，应是新转换器模型（`models/mcgm/zombie/zombie.mdl`，手臂垂下双足），不再 Valve Classic。① 走路：两腿应**反相前后摆**、手臂**前伸 + 轻微摆**（摆动方向/轴不对——前后变左右或乱转，调 `ApplyBMBBipedLocomotion` 的 Angle 分量；手臂没前伸/反了，调 `BipedArmForwardAngle` 符号/角度），速度越快摆越快/越大。② 攻击：进攻击范围挥手时手臂**前挥**（叠前伸基线、腿继续走），不再模型 gesture。③ 死亡：整只僵尸**侧躺**（绕 root，yaw；打转就换 pitch）+ Java poof，不跟跑/跟跳。④ 头：lookat/环视只控头、与移动并行。⑤ 行为不回归：远距离追击、chase_direct/A*/stalk、近战击退、ambient 叫。`UpdateBMBVisualBones` 摆轴/前伸/攻击/侧倒都是初值，按表现迭代。

0. **死亡序列必测**：侧躺 ✅、poof 形态/平滑 ✅、停留 1.5s ✅（用户认可）。本轮：① flee 跑动中 / 跳跃中死亡，尸体应**立即停住**、不再跟着跑或跟着跳（地面死正确静止；空中/跳跃中死会悬空倾倒，属已知小限制）；② poof 默认已上提 base，牛/猪等新 mob 死亡应开箱即用同款 Java poof。

0. **LookAt 必测**：玩家站在 sheep 附近时，羊应只是偶尔看一眼玩家（约 0.5s 轮询、6% 开始概率、每次 2-4s），不应玩家一靠近就持续盯住；head rot Z 上限从 35 降到 24，确认抬头/低头幅度是否自然。没看玩家时，静止/慢走应每 1-3s 小幅随机环视，偶尔看正前；快跑/Flee 时应朝正前，不要边跑边乱扫。看向期间 Wander/Flee/普通移动继续跑，身体移动不被打断；站在羊正前/左右/上方验证轴向：Head rot X 正应向左、负向右，Head rot Z 正向上、负向下；超出范围或计时结束后头应平滑回正；吃草和死亡时不应 lookat。

1. **上一轮已通过**：一格 hop 稳定，未再发现误上两格；debug 远点未再早停；跳后动作能复位。
2. **36.5 必测**：`lua_run print(MC and MC.BS, BMB and BMB.BS)` 应输出 36.5/36.5；一格 hop apex 约 `1.4~1.5*BS` 且稳定上台；两格必须上不去；半砖/楼梯应靠 StepHeight=28 走上去而不是 hop；36.5 宽走廊 hull 32 双向通行；drop 主动下 3 格（109.5u），4 格拒绝且不卡顿；吃草链路正常；`WorldToBlock/BlockToWorld` 新尺寸下往返一致；`bmb_world mock` 与 real 行为尺度一致。
3. **StrandedRecovery 必测**：物理枪/工具把羊放到玻璃板顶或挖掉脚下支撑后，HUD 应显示 `state=stranded`，随后短暂进入 `mode=stranded_bail`；撞障碍时应进入 `stranded_bail_retry` 并换方向，不应永久不动；若旁边没有合法地面，应侧向离开窄支撑并进入 `stranded_fall`，落到合法地面后恢复 wander。不得明显卡顿，也不得沿玻璃板网络当路走；普通 wander 不应主动把目标选到玻璃板顶。
4. **BlockHop 当前重点**：普通 path 不应再靠 carrot 抄近路跨一格空洞；可退场景中 `face_close` 应先 backoff 到理想起跳点；一格狭窄空间如果 backoff 被堵，HUD/log 应显示 `reason=blocked_close_lift backoffBlocked=true` 并直接起跳；如果理想 backoff 点能站但起跳会撞头，应显示 `backoffLift=false`，`effClose` 降到约 `0.48*BS` 并在较近的 lift-clear 点起跳；过贴脸仍不应硬跳。若物理把 hop 落到目标上方一格且 XY 已贴近节点，应推进路径而不是 debug_repath；复杂 debug 右键路径不应跑一半过期回 wander；第二十八轮的平滑移动和第二十七轮的性能/drop/debug/spawn idle 不回归。
5. **第三十轮已验证**：debug 右键目标若被一格空洞/死路隔开，已确认不再假死 ✅；这一修复必须保留。
6. **第三十一轮回滚/physgun 必测**：物理枪应恢复可抓取；抓起 sheep 放下后应能继续 wander，不需要打一枪/打一下才恢复；子弹/枪击应恢复掉血和受击反应；prop 物理伤害链路不受碰撞试验影响；玩家能踩/挤到 mob 按 GMod 手感接受；debug gap 不回归。
7. **第三十二轮必测**：受击进入 Flee 后 HUD `vel:实际/目标` 的目标速度不应在 run/walk 阈值上下大幅跳变；过弯 `path_corner` 可以略降速但不应降到走路动作；ACT_RUN 应在整个 panic 段保持稳定，直到 Flee 结束回 idle/wander。确认围住放弃、撞墙/悬崖保护、hop/drop、debug gap 不回归。
8. **第三十五轮已通过**：非冷却窗口枪击/近战命中扣血、红闪约 0.5s、冷却约 0.5s、水平击退 + 轻微上抬、击退后继续行为；HUD 不再出现目标速度永久 `0`。
9. **第三十七轮 Zombie 必测**：远距离识别到玩家后应持续推进追击，不应只显示 `chase` 但原地看；追击时 Classic zombie 腿部应播放走路序列；玩家在高一格平台上时，Zombie 不应因水平距离近进入 `attack_ready` 卡住，应继续找路/hop 到可达位置；同层攻击距离应比 38u 旧值宽松一些，约 52u 内能进入 windup，0.38s 后造成 10 HP；红闪应命中后立刻固定红约 0.5s，然后直接恢复，不再浅到深/深到浅渐变。
10. **第三十八轮 Zombie 必测**：攻击 windup/cooldown 期间 HUD 目标速度不应变 0，Zombie 应继续贴近目标；攻击频率应比 1.05s 原型快（当前 cooldown 0.8s、hit delay 0.28s）；玩家站高两格或当前无完整路径时，Zombie 可以到最近可达处/短暂 `chase_repath`，但不应清空目标长时间 idle/wander；复杂台阶 `path_hop` 必须真正触发跳跃或重试/失败，不应只显示 `path_hop` 后直接 `idle dist:0`。
11. **第三十九轮 Zombie 必测**：远距离追击应少出现“走几步停一下”，HUD 可短暂 `path_carrot`/`chase_repath` 但实体应继续给速度/朝目标推进；贴脸一格台阶/狭槽里 `path_hop` 应触发 `close_lift` 起跳或进入正常 retry/fail，不应长期 path_hop/chase_repath 来回切；高两格贴脸若确实没有路径可以停在最近可达处，但不应清空目标进入长 idle。
12. **第四十轮 Zombie 必测**：开阔地/看得到玩家时 HUD 应主要显示 `chase_direct`，Zombie 身体持续面向并直压玩家，不再平地到处拐弯；中间有墙、拐弯、迷宫或安全探测发现悬崖时应切回 BMB A*（`path_carrot/path_hop/path_drop`）绕路；玩家站高处且近距离不可达时应显示 `chase_stalk`，保持盯人/短周期重查，不清目标、不长时间 idle，也不再 `chase_repath` 零向量假动。
13. **Zombie Phase 2 必测**：约 1350u 内应能发现玩家，丢失距离约 1725u；同层进入约 60u 攻击范围后应立刻扣血，不再等 0.28s；连续攻击间隔约 1.0s；玩家直接踩在 Zombie 头上应因窄 overlap 分支挨打，但高一格平台/隔块目标仍应继续 chase/path，不应靠拉高 `AttackVerticalRange` 打空气；每次实际命中玩家应播放玩家受伤音效，并有轻微屏幕晃动；水平击退当前 `AttackKnockback=150`，击退+击飞合计目标约 2-3 格，贴脸 `dist:0.0` 时也不应时有时无；玩家站地面和跳起两种情况都应能看到竖直击飞，且不应重复整段水平推力；debug/held/stranded/chase/wander 任意状态下 Zombie 都会按 MC ambient 概率模型偶尔叫，不应只在行为循环空闲时叫；高台边缘/窄桥桥头直追玩家时应停在边缘显示 `chase_direct_cliff` / `attack_ready_cliff` / `chase_repath_cliff` 或回到 A*，Source 地图、prop、MCSWEP 方块边缘都不能无视断崖掉下去；完整 MCSWEP 方块平地/平台中间追击不应显示 `*_cliff` 停住。
14. **Zombie 声音必测**：ambient 偶尔播放 MC zombie `say1-3`；普通受击播放 `hurt1-2`，致死受击只播放 `death.ogg`，不应同时叠 hurt；实际咬中玩家播放 MC `damage/hit1-3`，不再是 Source `player/pl_pain*`；走/追击脚步按移动距离触发并尽量对齐双足落地，跑动自动更密，不能再听到固定 0.5s 的 `npc/zombie/foot1.wav`。
13. **hop hotfix 必测**：Sheep debug 右键一格台阶时可以显示 `path_hop`，但落地后应推进节点/到达目标，不应在 `path_hop` 与 `debug_repath` 之间循环；两格仍上不去，debug gap 不回归。
9. **Sheep 程序化腿摆 + 吃草低头必测（本轮迭代）**：idle 腿平稳收回；移动时腿摆幅随速度**连续缩放**，满幅 `25°`，`LimbSwingPhaseScale=0.09` 让走/跑频率都明显更慢，不再二元、不在阈值突跳；普通移动时头不随身体晃，也不应被每帧锁死，后续看向仍能接管；`AnimationSequences` 仍保持注释。吃草应按顺序：先头前伸下探（`pos Y` 到 -12）→ 低头到 `roll -55` 够到草地 → 在 -55↔-40 之间咀嚼两回 → `rot` 和 `pos` 一起均匀收回；约 0.45s 咬草（草变 dirt + 咀嚼音效），总时长约 1.8s，不再抬头/歪头/头朝玩家右转。
10. **Sheep 声音必测**：ambient say 应偶尔播放 MC 羊叫，受击也应是羊叫而不是 headcrab；走/跑脚步应按移动距离触发并和腿落地视觉同步，跑步自动更密，不应再是固定 0.5s 计时器；音量随速度略增、pitch 有随机；吃草咬草/变 dirt 那刻应随机播放 `dig grass1-4`，不再是 barnacle crunch。基础版脚步先用 sheep step OGG，后续若接方块 step 系统再按脚下 block 替换。
11. **回归仍需留意**：物理枪、子弹伤害、prop 伤害、绕路、窄沿、不卡顿、不跳楼、地图墙/拐弯、Flee 围住放弃、套皮后 activity。

## 未解 bug / 风险

- **【待办·暂搁置，用户要求先放放】debug tool 右键上台阶仍卡**：snap 目标 + carrot 不跨 hop 节点两个修复后，用户实测**仍**卡 `debug_move/debug_repath`、`node:0 adv:0 vel:0`，`bmb_chase_debug` 仍 `findpath full(N)` 反复刷（A* 给完整路径但 MoveAlongPath 走不动）。carrot-stops-at-vertical-node 没解决，说明卡点不止 carrot 一处（可能 hop 起跳准入/`IsBMBGridFootHullClear(current)` 让 IsPathGridVisible 整条否决/`MaintainBMBMoveSpeed` 与 hop 触发距离等）。下一轮要在 MoveAlongPath 各 fail/卡住出口加 reason 日志（path_cliff/path_wall/path_blocked/carrot_nil/hop_fail/ttl）精确定位是哪个出口/哪一帧，再对症修；不要再盲猜。wander/chase 能上、debug 远目标固定点不能上。
- **【待办·暂搁置，用户要求先放放】chase 在复杂台阶/高低差的剩余 A* 诊断**：已确认并修掉一类容易混淆的症状——敌对怪绕路时反复抄同一条直线撞 cliff，是 `chase_direct` 缺失败记忆，不是 A* 或 cliff 检测错（见本轮 direct cliff memory）。剩余只看真正 A* 产路/跟路问题：若仍卡 `chase_repath_cliff` + `vel:0` + `node:0`，开 `bmb_chase_debug 1` 看 `MoveToWorldPosition` 的 `findpath dist -> nil/partial(N)/full(N)`，再判断是搜索预算剪枝、goal 不可达，还是 MoveAlongPath 后续被拦；不动全局 A* 默认。
- **Zombie 程序化动画全是初值待实测**：双足摆轴默认 roll（Angle 第三分量，对齐羊腿；僵尸 leg/arm 局部轴若不同会变左右摆/乱转，需换 pitch/yaw 或符号）；手臂前伸 `BipedArmForwardAngle=-60`（roll，方向/角度猜测）；攻击前挥关键帧（roll 偏移）；死亡侧倒 yaw（复用羊 root 实测，僵尸 root 同 90°X bind 应一致但需确认）。都按游戏表现迭代，和羊腿摆/吃草/侧倒一样多轮调。僵尸家族手臂垂下是转换器全局改动（zombie/husk/drowned/pigman/baby_zombie），skeleton/wither 仍前伸（未做）。
- **空中/跳跃中死亡尸体悬空**：死亡 `SetSolid(SOLID_NONE)` 后靠重力落地会穿地，故死亡后每 tick `SetVelocity(0)+SetGravity(0)` 让尸体静止在死亡位置——空中或跳跃中死的会悬空倾倒。主流地面死亡正确。若要空中死落地，需在保持 solid（或单独 ground trace 落点 + 下沉补偿）下做，避免穿地。

- **死亡侧倒/悬空待确认**：实测 roll=后仰，已改 **yaw**（`Angle(0, ±tip, 0)`）+ `EntIndex` 左右随机；若 yaw 仍是打转，换 pitch（第一分量）。绕 root（脚底 pivot）转 90° 可能略悬空/穿地，明显则给死亡分支加向下补偿。`LookupBone("root")` 取不到则不倾倒（确认 mdl root 名仍是 `root`）。
- **poof `$frame` 逐粒子轮播待确认**：effect 用 `mc_poof:SetInt("$frame", n)` + `DrawQuadEasy` 逐粒子设帧。若 GMod 把同材质多个 quad 批处理成同一帧（所有粒子定格同帧），改用 sprite-sheet 单帧 VTF + mesh UV 切帧，或 `ParticleEmitter`。

- **吃草三段动画待游戏复测**：吃草已改回 roll 轴（实测俯仰=roll 负值，上轮误改 pitch 已回退），用 preview 实测值 `roll=-55`/`posY=-12` 做成下探→低头→咀嚼两回→收回的三段动画（`duration=1.8`、`biteDelay=0.45`）。若咀嚼快慢/够地深度/收回手感要调，改 `sheepAnimations.eat_grass.frames` 的时间点与 roll/posY 值即可，必要时用 `bmb_sheep_pose_preview` + `bmb_sheep_pose_print_keyframe` 重新取值。
- **腿摆连续缩放是手感参数，待游戏回归**：当前 sheep `legSwingMax=25.0` + `LimbSwingMinAmount=0.25` + `LimbSwingPhaseScale=0.09`，走路 amount≈`Lerp((speed-8)/(RunSpeed-8), 0.25, 1)`，跑步满幅 25°；若整体偏大/偏小调 `legSwingMax`，走路单独偏小调 `LimbSwingMinAmount`，走/跑频率调 `LimbSwingPhaseScale`/`LimbSwingFullSpeed`。牛/猪接 base helper 时各自的摆角/摆轴仍在自己实体里写。
- **低顶/头顶方块的坏 hop 仍待重新设计**：刚试过把 `hop` 邻接提前加 A* edge clearance，会导致正常 hop 大面积不触发，已按用户要求撤回；后续不能沿“全局剪 hop 边”硬做，应该在运动层失败记忆/局部绕路目标或更精确的低顶场景分诊里处理。
- **第二十四轮剩余风险**：代码层已按 36.5 参数化并通过静态检查/lint，但实际 GMod 场景还要按复测清单确认，尤其 hop apex、两格边界、半砖/楼梯和 36.5 走廊。
- **第二十五轮剩余风险**：StrandedRecovery 的移动阶段故意绕开 A* 和 `path_cliff`，但只做短距离 bail-out；若窄碰撞四周都被其它碰撞托住，可能显示 `stranded_no_escape` 并周期重试。高空玻璃板上会选择掉落式恢复，这是"已经被放到非法节点"的兜底，不代表普通 A* 允许跳崖。
- **第二十八轮剩余风险**：恢复 whole Think 每 tick 会拿回一点维护成本，但应该保留第 27 轮主要收益（A* / Wander / physics impact 的内部节流）。若 50 只仍不够，需要全局 AI/path queue，而不是再节流 entity Think。
- **第二十九轮剩余风险**：carrot 沿线 standable 检查每 tick 多做若干支撑查询，已在单次检查内共享 cache；若复杂路径性能变差，再考虑把 standable line check 降采样或缓存到当前 segment。
- **第三十一轮结论**：MC 式实体穿插/软推挤暂不做。`COLLISION_GROUP_PLAYER`、`ShouldCollide=false`、软分离都会威胁物理枪、子弹命中、prop 伤害等 GMod 基础交互；当前选择接受 GMod 手感，后续不要沿这条线继续调，除非先设计出不会影响 trace/physgun/damage 的分层方案。
- **第三十五轮剩余风险**：红闪用客户端 `render.SetColorModulation` 做整模型 tint，后续换 MC 模型/材质后要确认不会和皮肤材质冲突。竖直击退现在用 `loco:Jump()` + 直接 z 速度；若第一下仍偶尔不上抬，下一轮打受击 tick 日志（onGround、IsClimbingOrJumping、vel.z）确认是否被地面解算吞。摔伤尚未实现，后续应作为独立伤害来源，不触发击退。
- **第三十六轮剩余风险**：外部 `specV3_zombie_phase1.md` 因 H 盘读取审批工具故障未能直接读取，本轮按仓库现有 Phase 3/CLAUDE 架构约定实现最小纵向切片。Zombie 目标目前只锁玩家；日光燃烧、开门/破门、装备、村民/其它目标、声音频率细化和 MC 模型动画都未做。追击用短时间片 A* 重规划，移动玩家时观感需要实测再调。
- **第三十七轮剩余风险**：`RunActivity=ACT_WALK` 是针对当前 Classic zombie 占位模型的兼容映射；后续换 MC 模型后应改回模型自己的 run/walk 动作表。`AttackVerticalRange=28` 会阻止高一整格贴边攻击，符合本轮寻路需求，但如果后续想还原 MC 某些边缘能打到的 case，需要结合模型/碰撞盒再调。
- **第三十八轮剩余风险**：攻击期间用 `SteerTowards` 直接保持前压，只在近战范围内运行，未接完整 path safety；若贴玩家/平台边缘出现推挤或越边，需要把 attack pressure 收敛为更短距离的 path/direct helper。final hop 的脚底高度阈值当前是 8u，若真实 MCSWEP cell center/碰撞高度偏移导致成功 hop 不推进，再按 HUD 的 cell/z 数据调 `VerticalPathReachZTolerance`。
- **第三十九轮剩余风险**：`BlockHopAllowCloseLaunch` 只给 Zombie 开，用于狭槽里无法后退助跑的攻击性追击；如果出现贴墙弹跳/穿模，下一轮要在 close_lift 上加更严格的 Source hull clearance 或失败方向记忆。高处目标 XY 几乎重合时，`SteerTowards(target)` 的水平向量接近 0，若仍不动，需要给 chase_repath 加“高处贴脸目标的邻近 offset 点”而不是直接瞄玩家脚下。
- **第四十轮剩余风险**：`chase_direct` 依赖 `Visible(target)` + `IsMovementTargetSafe` 短探测；如果玩家隔着低矮障碍仍被判可见，Zombie 会先直压到障碍前再切 A*，这是预期的“看见就压迫，挡住再绕路”。`chase_stalk` 当前只保持贴底等待/盯人，不做环绕徘徊；如果后续想更像 MC 的小范围挤压，可以在 stalk 内加安全的近距离 offset，但不要重新清 target 或用零向量追击。
- **hop hotfix 剩余风险**：如果 HUD 仍显示 `hop ok` 后 `debug_repath`，下一步要打开 `bmb_debug_hop_log 1`，记录落地瞬间 `pos.z`、目标 foot z、XY 距离和失败 reason，分辨是“没认账”还是“认账后滑回/后续节点失败”。
- **第二十三轮剩余风险**：用户当前未发现 bug，但 StepHeight 临时切换、状态驱动 activity、debug 长路径 timeout 仍需要后续场景持续回归，尤其是套皮后动作映射。
- **hop 候选分诊**：若失败处 HUD 根本不进 `path_hop`，不要按弹道修；要开 A* hop 候选日志看是上方净空/支撑正确拒绝，还是邻接标记漏判。
- **PhysgunPickup 钩子语义**：它在"尝试抓取"时触发，若有第三方 addon 拒绝了这次拾取，BMBHeld 可能误置（下次松手钩子不来）；目前单机沙盒无此问题，联机装权限 addon 时留意。
- **半砖/MC 台阶对 A* 不可见**（`BlockIsFullCube`=false → BMB 当空气）：混半砖地形会选择跳整格或把半砖前方判成 cliff，且半砖不能作为 MC 支撑（目前靠 Source 不了 trace——MCSWEP 方块碰撞不是刷子，`MASK_SOLID_BRUSHONLY` 探不到）。需要 MCSWEP 暴露 shape / floor height（例如 `IBlockWorld:GetFloorHeight` 或 `MC.BlockBoxes`），再把 A* 的 walk/hop/drop 从二值格子改为真实表面高度差。
- **窄沿偏移采样的副作用**：紧挨沿边的悬空格可能因偏移样本擦到沿边被判可站立，理论上增加贴边坠落风险——`path_cliff` Source safety 仍兜底。
- **`HasSupport` 的 Source trace 语义**：若 MCSWEP cell 网格与 flatgrass 地面错位较大，站立层判定可能漂移一格；遇到再调 trace 区间。
- **partial path 改变了"路径失败"语义**：Wander/debug 移动遇到不可达目标会走到最近可达点算成功（MC 手感），只有 Flee 显式关闭（`allowPartial=false`）。
- 坑/封闭结构里的 Flee 仍是旧采样（随机世界点再验证），下一步改为枚举可站立格（复用 `HasSupport`）→ 随机抽 → A* 验证。
- path Source safety 的 `IsSourceHitBMBBlock` 采样偏移在 chunk 边界可能需要再调。
- 吃草粒子/动画：原版手感版待实现。
- `IsSolid` 粗略版：栅栏/半砖当可通过，撞住靠 watchdog 兜底。
- 性能：多只 mob 同时寻路未压测。

## 下一步

1. **复测 Skeleton M1（只判对错，不判手感）**：生成 `BMB Skeleton`（现已是骷髅模型）。看：识别玩家(16格)→`chase`接近→≤15格且稳定看见≥1s 切 `aim` **原地停下**（M1 不 strafe，不会风筝，正常）→满弓 1s→射箭（抛物线、命中扣~6HP、命中墙消失）→2s 冷却再射；打它一下记仇反击；有目标时头锁定玩家；shoot 音（crossbow 占位）响。**不评价**拉扯像不像样（M2 strafe 才做）。同时确认骷髅模型正面（不反 180）、手臂无目标垂下/有目标抬起（轴/值占位，按表现报）。回归：僵尸/羊不受影响（共享 `Flee.Run` 加了可选 threat 参数，不传时行为不变）。
2. Skeleton M2：strafe 风筝 + 安全探测、弓挂手骨、逃狼实测（需狼实体或占位）、左撇子 5%、polish（拉弦手/真箭/弓弦 bodygroup）。
3. 复测 `BMB Zombie`：`chase_direct` 开阔地直追、A* 绕路、`chase_stalk` 高处等待、近战、受击/held/debug/stranded 优先级、MC 音效。
4. 等朋友提供 shape/floor height 接口后，做半砖/楼梯/MC 台阶表面高度寻路。
5. 坑/走廊场景的 Flee 采样改为枚举可站立格后随机抽样。
6. 吃草原版粒子/动画/音效；怕人生物 `Avoid` 模块（参考 `AvoidEntityGoal.java`）。
